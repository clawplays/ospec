"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.ProjectTemplateBuilder = void 0;
const constants_1 = require("../../core/constants");
const fs_1 = require("fs");
const path = __importStar(require("path"));
const TemplateBuilderBase_1 = require("./TemplateBuilderBase");
class ProjectTemplateBuilder extends TemplateBuilderBase_1.TemplateBuilderBase {
    constructor(inputs) {
        super();
        this.inputs = inputs;
    }
    generateProjectReadmeTemplate(fallbackName, mode, input) {
        const context = this.getProjectContext(fallbackName, mode, input);
        return this.copy(context.documentLanguage, `# ${context.projectName}

> 由 OSpec CLI 初始化生成

## OSpec Bootstrap

当前仓库已按 \`${mode}\` 模式完成 OSpec 初始化。

## 项目简介

${context.summary}

## 项目知识层

- 根技能文档：\`SKILL.md\`
- 文档中心：\`docs/SKILL.md\`
- 可选知识地图：\`knowledge/src/SKILL.md\`、\`knowledge/tests/SKILL.md\`
- 语义索引：\`SKILL.index.json\`

## 执行层

Active change 位于 \`changes/active/<change>\`。
`, `# ${context.projectName}

> Generated by OSpec CLI

## OSpec Bootstrap

This repository has been initialized with OSpec in \`${mode}\` mode.

## Summary

${context.summary}

## Project Knowledge

- Root skill: \`SKILL.md\`
- Docs hub: \`docs/SKILL.md\`
- Optional knowledge maps: \`knowledge/src/SKILL.md\`, \`knowledge/tests/SKILL.md\`
- Index: \`SKILL.index.json\`

## Execution

Active changes live under \`changes/active/<change>\`.
`, `# ${context.projectName}

> OSpec CLI により生成

## OSpec Bootstrap

このリポジトリは OSpec の \`${mode}\` モードで初期化されました。

## 概要

${context.summary}

## プロジェクト知識

- ルート SKILL: \`SKILL.md\`
- docs ハブ: \`docs/SKILL.md\`
- 任意の知識マップ: \`knowledge/src/SKILL.md\`、\`knowledge/tests/SKILL.md\`
- インデックス: \`SKILL.index.json\`

## 実行レイヤー

active change は \`changes/active/<change>\` にあります。
`, `# ${context.projectName}

> تم توليده بواسطة OSpec CLI

## OSpec Bootstrap

تمت تهيئة هذا المستودع بواسطة OSpec في وضع \`${mode}\`.

## الملخص

${context.summary}

## معرفة المشروع

- ملف SKILL الجذري: \`SKILL.md\`
- مركز docs: \`docs/SKILL.md\`
- خرائط معرفة اختيارية: \`knowledge/src/SKILL.md\` و \`knowledge/tests/SKILL.md\`
- الفهرس: \`SKILL.index.json\`

## طبقة التنفيذ

توجد التغييرات النشطة تحت \`changes/active/<change>\` .
`);
    }
    generateRootSkillTemplate(fallbackName, mode, input) {
        const context = this.getProjectContext(fallbackName, mode, input);
        const body = this.copy(context.documentLanguage, `# ${context.projectName}

> 层级：第 1 层（项目根文档）

## 项目概述

- **项目名称**：${context.projectName}
- **模式**：${mode}
- **状态**：已完成 OSpec 初始化
- **简介**：${context.summary}

## 技术栈

${this.formatList(context.techStack, '待补充')}

## 项目架构

${context.architecture}

## 目录导航

- 文档中心：[docs/SKILL.md](docs/SKILL.md)
- AI 指南：[for-ai/ai-guide.md](for-ai/ai-guide.md)
- 可选知识地图：\`knowledge/src/SKILL.md\`、\`knowledge/tests/SKILL.md\`

## 插件阻断

- 开始推进 active change 前先读取 \`.skillrc\`。
- 如果项目启用了 Stitch，且当前 change 激活了 \`stitch_design_review\`，先检查 \`changes/active/<change>/artifacts/stitch/approval.json\`。
- 当 Stitch 审批缺失或状态不是 \`approved\` 时，视为 change 仍被阻断，先完成设计审核再继续。`, `# ${context.projectName}

> Layer: project root

## Project Overview

- **Project**: ${context.projectName}
- **Mode**: ${mode}
- **Status**: OSpec initialized
- **Summary**: ${context.summary}

## Tech Stack

${this.formatList(context.techStack, 'TBD')}

## Architecture

${context.architecture}

## Navigation

- Docs hub: [docs/SKILL.md](docs/SKILL.md)
- AI guide: [for-ai/ai-guide.md](for-ai/ai-guide.md)
- Optional knowledge maps: \`knowledge/src/SKILL.md\`, \`knowledge/tests/SKILL.md\`

## Plugin Gates

- Read \`.skillrc\` before advancing an active change.
- If Stitch is enabled and the current change activates \`stitch_design_review\`, inspect \`changes/active/<change>/artifacts/stitch/approval.json\`.
- When Stitch approval is missing or its status is not \`approved\`, treat the change as blocked until design review is complete.`, `# ${context.projectName}

> レイヤー: プロジェクトルート

## プロジェクト概要

- **プロジェクト**: ${context.projectName}
- **モード**: ${mode}
- **状態**: OSpec により初期化済み
- **概要**: ${context.summary}

## 技術スタック

${this.formatList(context.techStack, '未定')}

## アーキテクチャ

${context.architecture}

## ナビゲーション

- docs ハブ: [docs/SKILL.md](docs/SKILL.md)
- AI ガイド: [for-ai/ai-guide.md](for-ai/ai-guide.md)
- 任意の知識マップ: \`knowledge/src/SKILL.md\`、\`knowledge/tests/SKILL.md\`

## プラグインゲート

- active change を進める前に \`.skillrc\` を読む。
- Stitch が有効で、現在の change が \`stitch_design_review\` を有効化している場合は \`changes/active/<change>/artifacts/stitch/approval.json\` を確認する。
- Stitch 承認がない、または状態が \`approved\` でない場合は、デザインレビュー完了まで change をブロックされたものとして扱う。`, `# ${context.projectName}

> الطبقة: جذر المشروع

## نظرة عامة على المشروع

- **المشروع**: ${context.projectName}
- **النمط**: ${mode}
- **الحالة**: تمت تهيئته بواسطة OSpec
- **الملخص**: ${context.summary}

## المكدس التقني

${this.formatList(context.techStack, 'قيد التحديد')}

## المعمارية

${context.architecture}

## التنقل

- مركز docs: [docs/SKILL.md](docs/SKILL.md)
- دليل الذكاء الاصطناعي: [for-ai/ai-guide.md](for-ai/ai-guide.md)
- خرائط معرفة اختيارية: \`knowledge/src/SKILL.md\` و \`knowledge/tests/SKILL.md\`

## بوابات الإضافات

- اقرأ \`.skillrc\` قبل متابعة أي change نشط.
- إذا كان Stitch مفعلاً وكان change الحالي يفعّل \`stitch_design_review\`، فافحص \`changes/active/<change>/artifacts/stitch/approval.json\`.
- عندما تكون موافقة Stitch مفقودة أو لا تكون حالتها \`approved\`، فاعتبر change محجوباً حتى يكتمل مراجعة التصميم.`);
        return this.withFrontmatter({
            name: context.projectName,
            title: this.copy(context.documentLanguage, context.projectName, `${context.projectName} Project`, `${context.projectName} プロジェクト`, `${context.projectName} المشروع`),
            tags: ['ospec', 'project', mode],
        }, body);
    }
    generateDocsSkillTemplate(fallbackName, input) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const body = this.copy(context.documentLanguage, `# 文档中心

> 层级：第 2 层（项目文档索引）

## 文档导航

- 项目概览：[project/overview.md](project/overview.md)
- 技术栈：[project/tech-stack.md](project/tech-stack.md)
- 架构说明：[project/architecture.md](project/architecture.md)
- 模块地图：[project/module-map.md](project/module-map.md)
- API 总览：[project/api-overview.md](project/api-overview.md)
- 设计文档目录：[design/README.md](design/README.md)
- 计划文档目录：[planning/README.md](planning/README.md)
- API 文档目录：[api/README.md](api/README.md)`, `# Documentation Hub

> Layer: docs index

## Navigation

- Project overview: [project/overview.md](project/overview.md)
- Tech stack: [project/tech-stack.md](project/tech-stack.md)
- Architecture: [project/architecture.md](project/architecture.md)
- Module map: [project/module-map.md](project/module-map.md)
- API overview: [project/api-overview.md](project/api-overview.md)
- Design docs: [design/README.md](design/README.md)
- Planning docs: [planning/README.md](planning/README.md)
- API docs: [api/README.md](api/README.md)`, `# ドキュメントハブ

> レイヤー: docs インデックス

## ナビゲーション

- プロジェクト概要: [project/overview.md](project/overview.md)
- 技術スタック: [project/tech-stack.md](project/tech-stack.md)
- アーキテクチャ: [project/architecture.md](project/architecture.md)
- モジュールマップ: [project/module-map.md](project/module-map.md)
- API 概要: [project/api-overview.md](project/api-overview.md)
- 設計文書: [design/README.md](design/README.md)
- 計画文書: [planning/README.md](planning/README.md)
- API 文書: [api/README.md](api/README.md)`, `# مركز الوثائق

> الطبقة: فهرس docs

## التنقل

- نظرة عامة على المشروع: [project/overview.md](project/overview.md)
- المكدس التقني: [project/tech-stack.md](project/tech-stack.md)
- المعمارية: [project/architecture.md](project/architecture.md)
- خريطة الوحدات: [project/module-map.md](project/module-map.md)
- نظرة عامة على API: [project/api-overview.md](project/api-overview.md)
- وثائق التصميم: [design/README.md](design/README.md)
- وثائق التخطيط: [planning/README.md](planning/README.md)
- وثائق API: [api/README.md](api/README.md)`);
        return this.withFrontmatter({
            name: 'docs',
            title: this.copy(context.documentLanguage, `${context.projectName} 文档中心`, `${context.projectName} Docs Hub`, `${context.projectName} ドキュメントハブ`, `${context.projectName} مركز الوثائق`),
            tags: ['docs', 'project', 'planning', 'api'],
        }, body);
    }
    generateSrcSkillTemplate(fallbackName, input) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const moduleLinks = this.formatLinkedList(context.modulePlans.map(plan => ({
            displayName: plan.displayName,
            path: plan.path
                .replace(`${constants_1.DIR_NAMES.KNOWLEDGE}/${constants_1.DIR_NAMES.SRC}/`, '')
                .replace(`${constants_1.DIR_NAMES.SRC}/`, ''),
        })), this.copy(context.documentLanguage, '待补充', 'TBD', '未定', 'قيد التحديد'));
        const body = this.copy(context.documentLanguage, `# 源码地图

> 层级：第 2 层（源码索引）

## 目录导航

- 核心层：[core/SKILL.md](core/SKILL.md)
- 模块层：\`knowledge/src/modules/<module>/SKILL.md\`

## 模块说明

${moduleLinks}`, `# Source Map

> Layer: source index

## Navigation

- Core layer: [core/SKILL.md](core/SKILL.md)
- Module layer: \`knowledge/src/modules/<module>/SKILL.md\`

## Modules

${moduleLinks}`, `# ソースマップ

> レイヤー: ソースインデックス

## ナビゲーション

- コア層: [core/SKILL.md](core/SKILL.md)
- モジュール層: \`knowledge/src/modules/<module>/SKILL.md\`

## モジュール

${moduleLinks}`, `# خريطة المصدر

> الطبقة: فهرس المصدر

## التنقل

- الطبقة الأساسية: [core/SKILL.md](core/SKILL.md)
- طبقة الوحدات: \`knowledge/src/modules/<module>/SKILL.md\`

## الوحدات

${moduleLinks}`);
        return this.withFrontmatter({
            name: 'src',
            title: this.copy(context.documentLanguage, `${context.projectName} 源码地图`, `${context.projectName} Source Map`, `${context.projectName} ソースマップ`, `${context.projectName} خريطة المصدر`),
            tags: ['src', 'modules', 'architecture'],
        }, body);
    }
    generateCoreSkillTemplate(fallbackName, input) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const body = this.copy(context.documentLanguage, `# Core 核心层

> 层级：第 3 层（核心模块）
> 上层：[knowledge/src/SKILL.md](../SKILL.md)

## 模块概述

- **职责**：承载项目级公共能力
- **位置**：src/core/
- **依赖**：待补充

## API 文档

- 项目 API 总览：[../../../docs/project/api-overview.md](../../../docs/project/api-overview.md)`, `# Core Layer

> Layer: core module
> Parent: [knowledge/src/SKILL.md](../SKILL.md)

## Module Overview

- **Role**: shared runtime and infrastructure capabilities
- **Path**: src/core/
- **Dependencies**: TBD

## API Docs

- Project API overview: [../../../docs/project/api-overview.md](../../../docs/project/api-overview.md)`, `# コア層

> レイヤー: コアモジュール
> 親: [knowledge/src/SKILL.md](../SKILL.md)

## モジュール概要

- **役割**: 共通ランタイムおよび基盤機能
- **パス**: src/core/
- **依存関係**: 未定

## API 文書

- プロジェクト API 概要: [../../../docs/project/api-overview.md](../../../docs/project/api-overview.md)`, `# الطبقة الأساسية

> الطبقة: الوحدة الأساسية
> الأصل: [knowledge/src/SKILL.md](../SKILL.md)

## نظرة عامة على الوحدة

- **الدور**: القدرات المشتركة للتشغيل والبنية التحتية
- **المسار**: src/core/
- **الاعتماديات**: قيد التحديد

## وثائق API

- نظرة عامة على API للمشروع: [../../../docs/project/api-overview.md](../../../docs/project/api-overview.md)`);
        return this.withFrontmatter({
            name: 'core',
            title: this.copy(context.documentLanguage, `${context.projectName} 核心层`, `${context.projectName} Core Layer`, `${context.projectName} コア層`, `${context.projectName} الطبقة الأساسية`),
            tags: ['core', 'runtime', 'infrastructure'],
        }, body);
    }
    generateTestsSkillTemplate(fallbackName, input) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const body = this.copy(context.documentLanguage, `# 测试说明

## 测试策略

- 单元测试：待补充
- 集成测试：待补充
- 端到端测试：待补充`, `# Test Guide

## Test Strategy

- Unit tests: TBD
- Integration tests: TBD
- End-to-end tests: TBD`, `# テストガイド

## テスト戦略

- 単体テスト: 未定
- 統合テスト: 未定
- エンドツーエンドテスト: 未定`, `# دليل الاختبارات

## استراتيجية الاختبار

- اختبارات الوحدة: قيد التحديد
- اختبارات التكامل: قيد التحديد
- اختبارات طرف إلى طرف: قيد التحديد`);
        return this.withFrontmatter({
            name: 'tests',
            title: this.copy(context.documentLanguage, `${context.projectName} 测试说明`, `${context.projectName} Test Guide`, `${context.projectName} テストガイド`, `${context.projectName} دليل الاختبارات`),
            tags: ['tests', 'quality', 'verification'],
        }, body);
    }
    generateProjectOverviewTemplate(fallbackName, mode, input) {
        const context = this.getProjectContext(fallbackName, mode, input);
        const body = this.copy(context.documentLanguage, `# 项目概览

## 背景

\`${context.projectName}\` 已通过 OSpec CLI 初始化，当前模式为 \`${mode}\`。

## 项目简介

${context.summary}

## 当前目标

- 保持项目知识文档与仓库真实状态一致
- 建立分层 SKILL 体系
- 使用 change workflow 管理需求交付`, `# Project Overview

## Background

\`${context.projectName}\` has been initialized by OSpec CLI in \`${mode}\` mode.

## Summary

${context.summary}

## Current Goals

- Keep the project knowledge docs aligned with the real repository state
- Establish layered skill files
- Manage delivery through the change workflow`, `# プロジェクト概要

## 背景

\`${context.projectName}\` は OSpec CLI により \`${mode}\` モードで初期化されました。

## 概要

${context.summary}

## 現在の目標

- プロジェクト知識文書を実際のリポジトリ状態と一致させる
- レイヤー化された SKILL を整備する
- change workflow で要件の実行を管理する`, `# نظرة عامة على المشروع

## الخلفية

تمت تهيئة \`${context.projectName}\` بواسطة OSpec CLI في وضع \`${mode}\`.

## الملخص

${context.summary}

## الأهداف الحالية

- إبقاء وثائق معرفة المشروع متوافقة مع الحالة الفعلية للمستودع
- تأسيس ملفات SKILL متعددة الطبقات
- إدارة التسليم عبر change workflow`);
        return this.withFrontmatter({
            name: 'project-overview',
            title: this.copy(context.documentLanguage, `${context.projectName} 项目概览`, `${context.projectName} Project Overview`, `${context.projectName} プロジェクト概要`, `${context.projectName} نظرة عامة على المشروع`),
            tags: ['project', 'overview', 'bootstrap'],
        }, body);
    }
    generateTechStackTemplate(fallbackName, input) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const body = this.copy(context.documentLanguage, `# 技术栈

## 项目

- **项目名称**：${context.projectName}

## 技术栈清单

${this.formatList(context.techStack, '待补充')}`, `# Tech Stack

## Project

- **Project**: ${context.projectName}

## Stack List

${this.formatList(context.techStack, 'TBD')}`, `# 技術スタック

## プロジェクト

- **プロジェクト**: ${context.projectName}

## スタック一覧

${this.formatList(context.techStack, '未定')}`, `# المكدس التقني

## المشروع

- **المشروع**: ${context.projectName}

## قائمة المكدس

${this.formatList(context.techStack, 'قيد التحديد')}`);
        return this.withFrontmatter({
            name: 'tech-stack',
            title: this.copy(context.documentLanguage, `${context.projectName} 技术栈`, `${context.projectName} Tech Stack`, `${context.projectName} 技術スタック`, `${context.projectName} المكدس التقني`),
            tags: ['project', 'tech-stack', 'architecture'],
        }, body);
    }
    generateArchitectureTemplate(fallbackName, input) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const body = this.copy(context.documentLanguage, `# 架构说明

## 整体架构

- **项目**：${context.projectName}
- **当前阶段**：已建立 OSpec 项目知识骨架

${context.architecture}

## 分层说明

- Project Knowledge
- Layered Skills
- Execution
- GUI`, `# Architecture

## System Architecture

- **Project**: ${context.projectName}
- **Current phase**: OSpec knowledge skeleton initialized

${context.architecture}

## Layers

- Project Knowledge
- Layered Skills
- Execution
- GUI`, `# アーキテクチャ

## システムアーキテクチャ

- **プロジェクト**: ${context.projectName}
- **現在の段階**: OSpec の知識骨格を初期化済み

${context.architecture}

## レイヤー

- Project Knowledge
- Layered Skills
- Execution
- GUI`, `# المعمارية

## معمارية النظام

- **المشروع**: ${context.projectName}
- **المرحلة الحالية**: تم تهيئة هيكل معرفة OSpec

${context.architecture}

## الطبقات

- Project Knowledge
- Layered Skills
- Execution
- GUI`);
        return this.withFrontmatter({
            name: 'architecture',
            title: this.copy(context.documentLanguage, `${context.projectName} 架构说明`, `${context.projectName} Architecture`, `${context.projectName} アーキテクチャ`, `${context.projectName} المعمارية`),
            tags: ['project', 'architecture', 'layers'],
        }, body);
    }
    generateModuleMapTemplate(fallbackName, input) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const modules = this.formatLinkedList(context.modulePlans.map(plan => ({
            displayName: plan.displayName,
            path: plan.path,
        })), this.copy(context.documentLanguage, '待补充', 'TBD', '未定', 'قيد التحديد'));
        const body = this.copy(context.documentLanguage, `# 模块地图

## 项目

- **项目名称**：${context.projectName}

## 模块列表

${modules}`, `# Module Map

## Project

- **Project**: ${context.projectName}

## Modules

${modules}`, `# モジュールマップ

## プロジェクト

- **プロジェクト**: ${context.projectName}

## モジュール

${modules}`, `# خريطة الوحدات

## المشروع

- **المشروع**: ${context.projectName}

## الوحدات

${modules}`);
        return this.withFrontmatter({
            name: 'module-map',
            title: this.copy(context.documentLanguage, `${context.projectName} 模块地图`, `${context.projectName} Module Map`, `${context.projectName} モジュールマップ`, `${context.projectName} خريطة الوحدات`),
            tags: ['project', 'modules', 'map'],
        }, body);
    }
    generateApiOverviewTemplate(fallbackName, input) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const apiAreas = this.formatLinkedList(context.apiAreaPlans.map(plan => ({
            displayName: plan.displayName,
            path: plan.path,
        })), this.copy(context.documentLanguage, '待补充', 'TBD', '未定', 'قيد التحديد'));
        const moduleApis = this.formatLinkedList(context.moduleApiPlans.map(plan => ({
            displayName: plan.displayName,
            path: plan.path,
        })), this.copy(context.documentLanguage, '待补充', 'TBD', '未定', 'قيد التحديد'));
        const body = this.copy(context.documentLanguage, `# API 总览

## 项目

- **项目名称**：${context.projectName}

## API 边界

${apiAreas}

## 模块 API 文档

${moduleApis}`, `# API Overview

## Project

- **Project**: ${context.projectName}

## API Areas

${apiAreas}

## Module API Docs

${moduleApis}`, `# API 概要

## プロジェクト

- **プロジェクト**: ${context.projectName}

## API 領域

${apiAreas}

## モジュール API 文書

${moduleApis}`, `# نظرة عامة على API

## المشروع

- **المشروع**: ${context.projectName}

## نطاقات API

${apiAreas}

## وثائق API للوحدات

${moduleApis}`);
        return this.withFrontmatter({
            name: 'api-overview',
            title: this.copy(context.documentLanguage, `${context.projectName} API 总览`, `${context.projectName} API Overview`, `${context.projectName} API 概要`, `${context.projectName} نظرة عامة على API`),
            tags: ['project', 'api', 'overview'],
        }, body);
    }
    generateDesignDocsTemplate(fallbackName, input) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const designDocs = this.formatLinkedList(context.designDocPlans.map(plan => ({
            displayName: plan.displayName,
            path: plan.path,
        })), this.copy(context.documentLanguage, '待补充', 'TBD', '未定', 'قيد التحديد'));
        const body = this.copy(context.documentLanguage, `# 设计文档规划

## 项目

- **项目名称**：${context.projectName}

## 需要沉淀的设计文档

${designDocs}`, `# Design Docs

## Project

- **Project**: ${context.projectName}

## Planned Design Docs

${designDocs}`, `# 設計文書

## プロジェクト

- **プロジェクト**: ${context.projectName}

## 計画中の設計文書

${designDocs}`, `# وثائق التصميم

## المشروع

- **المشروع**: ${context.projectName}

## وثائق التصميم المخطط لها

${designDocs}`);
        return this.withFrontmatter({
            name: 'design-docs',
            title: this.copy(context.documentLanguage, `${context.projectName} 设计文档`, `${context.projectName} Design Docs`, `${context.projectName} 設計文書`, `${context.projectName} وثائق التصميم`),
            tags: ['docs', 'design', 'planning'],
        }, body);
    }
    generatePlanningDocsTemplate(fallbackName, input) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const planningDocs = this.formatLinkedList(context.planningDocPlans.map(plan => ({
            displayName: plan.displayName,
            path: plan.path,
        })), this.copy(context.documentLanguage, '待补充', 'TBD', '未定', 'قيد التحديد'));
        const body = this.copy(context.documentLanguage, `# 开发计划文档

## 项目

- **项目名称**：${context.projectName}

## 计划文档需求

${planningDocs}`, `# Planning Docs

## Project

- **Project**: ${context.projectName}

## Planning Documents

${planningDocs}`, `# 計画文書

## プロジェクト

- **プロジェクト**: ${context.projectName}

## 計画文書

${planningDocs}`, `# وثائق التخطيط

## المشروع

- **المشروع**: ${context.projectName}

## وثائق التخطيط

${planningDocs}`);
        return this.withFrontmatter({
            name: 'planning-docs',
            title: this.copy(context.documentLanguage, `${context.projectName} 计划文档`, `${context.projectName} Planning Docs`, `${context.projectName} 計画文書`, `${context.projectName} وثائق التخطيط`),
            tags: ['docs', 'planning', 'milestone'],
        }, body);
    }
    generateApiDocsTemplate(fallbackName, input) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const apiDocs = this.formatLinkedList(context.apiAreaPlans.map(plan => ({
            displayName: plan.displayName,
            path: plan.path,
        })), this.copy(context.documentLanguage, '待补充', 'TBD', '未定', 'قيد التحديد'));
        const body = this.copy(context.documentLanguage, `# API 文档目录说明

## 项目

- **项目名称**：${context.projectName}

## API 文档规划

${apiDocs}`, `# API Docs Directory

## Project

- **Project**: ${context.projectName}

## Planned API Docs

${apiDocs}`, `# API 文書ディレクトリ

## プロジェクト

- **プロジェクト**: ${context.projectName}

## 計画中の API 文書

${apiDocs}`, `# دليل وثائق API

## المشروع

- **المشروع**: ${context.projectName}

## وثائق API المخطط لها

${apiDocs}`);
        return this.withFrontmatter({
            name: 'api-docs',
            title: this.copy(context.documentLanguage, `${context.projectName} API 文档`, `${context.projectName} API Docs`, `${context.projectName} API 文書`, `${context.projectName} وثائق API`),
            tags: ['docs', 'api', 'reference'],
        }, body);
    }
    generateModuleSkillTemplate(fallbackName, moduleName, input, moduleSlug) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const slug = moduleSlug || this.slugify(moduleName) || 'module';
        const matchedModuleApiDoc = context.moduleApiPlans.find(plan => plan.name === `module-${slug}`);
        const matchedApiDocs = context.apiAreaPlans.filter(plan => plan.name.includes(slug) ||
            plan.displayName.includes(moduleName) ||
            moduleName.includes(plan.displayName.replace(/\s+api$/i, '')));
        const refs = [
            ...(matchedModuleApiDoc
                ? [{ title: `${moduleName} module api`, path: matchedModuleApiDoc.path }]
                : []),
            ...matchedApiDocs.map(plan => ({ title: plan.displayName, path: plan.path })),
        ];
        const presetBody = this.getPresetModuleSkillBody(context, moduleName, slug, refs);
        const body = presetBody ?? this.copy(context.documentLanguage, `# ${moduleName} 模块

> 层级：第 3 层（业务模块文档）
> 上层：[knowledge/src/SKILL.md](../../SKILL.md)

## 模块概述

- **项目**：${context.projectName}
- **模块名**：${moduleName}
- **路径**：src/modules/${slug}

## 主要职责

- 承载 ${moduleName} 相关业务能力
- 衔接项目级设计与当前模块实现
- 维护模块边界、依赖与测试要求

## API 文档

${this.formatReferenceList(refs, '待补充')}

## 依赖关系

- 上游依赖：\`src/core/\`、项目级配置、共享基础设施
- 同层协作：与其他 \`src/modules/*\` 保持清晰边界，通过文档和接口约定协作
- 下游影响：实现变更后需回看关联 API 文档、执行层 change 文档和测试说明

## 测试要求

- 单元测试：覆盖 ${moduleName} 模块核心业务规则和边界分支
- 集成测试：覆盖该模块与 API / 数据层 / 外部服务的集成路径
- 回归验证：当模块接口或行为变化时，同步更新 change 验证与相关文档

## 关联文档

- 项目模块地图：[../../../../docs/project/module-map.md](../../../../docs/project/module-map.md)
- API 总览：[../../../../docs/project/api-overview.md](../../../../docs/project/api-overview.md)
- 模块源码入口：当前目录`, `# ${moduleName}

> Layer: module document
> Parent: [knowledge/src/SKILL.md](../../SKILL.md)

## Module Overview

- **Project**: ${context.projectName}
- **Module**: ${moduleName}
- **Path**: src/modules/${slug}

## Responsibilities

- Own ${moduleName} domain behavior
- Connect project design and implementation in this module
- Maintain module boundaries, dependencies, and tests

## API Docs

${this.formatReferenceList(refs, 'TBD')}

## Dependencies

- Upstream: \`src/core/\`, project configuration, shared infrastructure
- Peer collaboration: keep clear boundaries with other \`src/modules/*\`
- Downstream impact: update API docs, change docs, and tests when behavior changes

## Testing Requirements

- Unit tests: cover core business rules and edge cases
- Integration tests: cover module interactions with APIs, data, and services
- Regression checks: align change verification when interfaces change

## Related Docs

- Module map: [../../../../docs/project/module-map.md](../../../../docs/project/module-map.md)
- API overview: [../../../../docs/project/api-overview.md](../../../../docs/project/api-overview.md)`, `# ${moduleName} モジュール

> レイヤー: モジュール文書
> 親: [knowledge/src/SKILL.md](../../SKILL.md)

## モジュール概要

- **プロジェクト**: ${context.projectName}
- **モジュール**: ${moduleName}
- **パス**: src/modules/${slug}

## 責務

- ${moduleName} ドメインの振る舞いを担う
- このモジュールでプロジェクト設計と実装を接続する
- モジュール境界、依存関係、テスト要件を維持する

## API 文書

${this.formatReferenceList(refs, '未定')}

## 依存関係

- 上流: \`src/core/\`、プロジェクト設定、共有基盤
- 同列協調: 他の \`src/modules/*\` と明確な境界を保つ
- 下流影響: 振る舞い変更時は API 文書、change 文書、テストを更新する

## テスト要件

- 単体テスト: 主要な業務ルールと境界条件をカバーする
- 統合テスト: API、データ、サービスとの連携をカバーする
- リグレッション確認: インターフェース変更時に change 検証を同期する

## 関連文書

- モジュールマップ: [../../../../docs/project/module-map.md](../../../../docs/project/module-map.md)
- API 概要: [../../../../docs/project/api-overview.md](../../../../docs/project/api-overview.md)`, `# وحدة ${moduleName}

> الطبقة: وثيقة الوحدة
> الأصل: [knowledge/src/SKILL.md](../../SKILL.md)

## نظرة عامة على الوحدة

- **المشروع**: ${context.projectName}
- **الوحدة**: ${moduleName}
- **المسار**: src/modules/${slug}

## المسؤوليات

- امتلاك سلوك نطاق ${moduleName}
- ربط تصميم المشروع بالتنفيذ داخل هذه الوحدة
- الحفاظ على حدود الوحدة واعتمادياتها واختباراتها

## وثائق API

${this.formatReferenceList(refs, 'قيد التحديد')}

## الاعتماديات

- المنبع: \`src/core/\` وإعدادات المشروع والبنية المشتركة
- التعاون الموازي: الحفاظ على حدود واضحة مع \`src/modules/*\` الأخرى
- الأثر اللاحق: حدّث وثائق API ووثائق change والاختبارات عند تغيّر السلوك

## متطلبات الاختبار

- اختبارات الوحدة: تغطي القواعد الأساسية والحالات الحدّية
- اختبارات التكامل: تغطي تفاعل الوحدة مع API والبيانات والخدمات
- فحوص الارتداد: وافق التحقق مع change عند تغيّر الواجهات

## وثائق ذات صلة

- خريطة الوحدات: [../../../../docs/project/module-map.md](../../../../docs/project/module-map.md)
- نظرة عامة على API: [../../../../docs/project/api-overview.md](../../../../docs/project/api-overview.md)`);
        return this.withFrontmatter({
            name: slug,
            title: this.copy(context.documentLanguage, `${context.projectName} ${moduleName} 模块`, `${context.projectName} ${moduleName} Module`, `${context.projectName} ${moduleName} モジュール`, `${context.projectName} وحدة ${moduleName}`),
            tags: ['module', slug, 'domain'],
        }, body);
    }
    generateApiAreaDocTemplate(fallbackName, apiAreaName, input) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const presetBody = this.getPresetApiAreaDocBody(context, apiAreaName);
        const body = presetBody ?? this.copy(context.documentLanguage, `# ${apiAreaName}

## 项目

- **项目名称**：${context.projectName}

## 边界说明

- 负责 ${apiAreaName} 相关接口能力
- 待补充请求入口、鉴权规则、错误码和数据模型

## 待补充清单

- [ ] 路由列表
- [ ] 请求/响应结构
- [ ] 权限与安全要求
- [ ] 测试与验收要求`, `# ${apiAreaName}

## Project

- **Project**: ${context.projectName}

## Boundary

- Own ${apiAreaName} interfaces and contracts
- Document entrypoints, auth rules, error codes, and data models

## Checklist

- [ ] Routes
- [ ] Request / response payloads
- [ ] Permissions and security rules
- [ ] Tests and acceptance requirements`, `# ${apiAreaName}

## プロジェクト

- **プロジェクト**: ${context.projectName}

## 境界

- ${apiAreaName} のインターフェースと契約を担当する
- エントリポイント、認証ルール、エラーコード、データモデルを文書化する

## チェックリスト

- [ ] ルート
- [ ] リクエスト / レスポンス
- [ ] 権限とセキュリティルール
- [ ] テストと受け入れ要件`, `# ${apiAreaName}

## المشروع

- **المشروع**: ${context.projectName}

## الحدود

- امتلاك واجهات وعقود ${apiAreaName}
- توثيق نقاط الدخول وقواعد المصادقة ورموز الأخطاء ونماذج البيانات

## قائمة التحقق

- [ ] المسارات
- [ ] هياكل الطلب / الاستجابة
- [ ] قواعد الأذونات والأمان
- [ ] متطلبات الاختبار والقبول`);
        return this.withFrontmatter({
            name: this.slugify(apiAreaName) || 'api-area',
            title: this.copy(context.documentLanguage, `${apiAreaName} API 文档`, `${apiAreaName} API Doc`, `${apiAreaName} API 文書`, `وثيقة ${apiAreaName} API`),
            tags: ['api', 'reference', 'module'],
        }, body);
    }
    generateModuleApiDocTemplate(fallbackName, moduleName, input, moduleSlug) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const slug = moduleSlug || this.slugify(moduleName) || 'module';
        const presetBody = this.getPresetModuleApiDocBody(context, moduleName, slug);
        const body = presetBody ?? this.copy(context.documentLanguage, `# ${moduleName} Module API

## 项目

- **项目名称**：${context.projectName}
- **模块名**：${moduleName}
- **模块路径**：src/modules/${slug}

## 对外接口

- 待补充该模块暴露的服务、事件、命令或接口

## 依赖关系

- 上游依赖：待补充
- 下游调用方：待补充
- 共享依赖：待补充

## 测试要求

- 单元测试：核心逻辑和边界条件
- 集成测试：跨模块 / API / 数据依赖
- 回归验证：接口变更后同步更新 change 验证`, `# ${moduleName} Module API

## Project

- **Project**: ${context.projectName}
- **Module**: ${moduleName}
- **Module path**: src/modules/${slug}

## Public Interface

- Document the services, events, commands, or APIs exposed by this module

## Dependencies

- Upstream: TBD
- Downstream consumers: TBD
- Shared dependencies: TBD

## Testing Requirements

- Unit tests: core logic and edge cases
- Integration tests: cross-module / API / data dependencies
- Regression checks: update change verification when interfaces change`, `# ${moduleName} モジュール API

## プロジェクト

- **プロジェクト**: ${context.projectName}
- **モジュール**: ${moduleName}
- **モジュールパス**: src/modules/${slug}

## 公開インターフェース

- このモジュールが公開するサービス、イベント、コマンド、API を文書化する

## 依存関係

- 上流: 未定
- 下流利用側: 未定
- 共有依存: 未定

## テスト要件

- 単体テスト: 主要ロジックと境界条件
- 統合テスト: モジュール横断 / API / データ依存
- リグレッション確認: インターフェース変更時に change 検証を更新する`, `# API وحدة ${moduleName}

## المشروع

- **المشروع**: ${context.projectName}
- **الوحدة**: ${moduleName}
- **مسار الوحدة**: src/modules/${slug}

## الواجهة العامة

- وثّق الخدمات أو الأحداث أو الأوامر أو واجهات API التي تكشفها هذه الوحدة

## الاعتماديات

- المنبع: قيد التحديد
- الجهات المستهلكة: قيد التحديد
- الاعتماديات المشتركة: قيد التحديد

## متطلبات الاختبار

- اختبارات الوحدة: المنطق الأساسي والحالات الحدّية
- اختبارات التكامل: الاعتماديات العابرة للوحدات / API / البيانات
- فحوص الارتداد: حدّث تحقق change عند تغيّر الواجهات`);
        return this.withFrontmatter({
            name: `module-${slug}`,
            title: this.copy(context.documentLanguage, `${moduleName} 模块 API`, `${moduleName} Module API`, `${moduleName} モジュール API`, `API وحدة ${moduleName}`),
            tags: ['api', 'module', slug],
        }, body);
    }
    generateDesignDocTemplate(fallbackName, docName, input) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const presetBody = this.getPresetDesignDocBody(context, docName);
        const body = presetBody ?? this.copy(context.documentLanguage, `# ${docName}

## 项目

- **项目名称**：${context.projectName}

## 设计目标

- 说明 ${docName} 的设计背景和目标
- 记录方案边界、关键约束和取舍

## 待补充内容

- 背景
- 方案
- 风险
- 验证方式`, `# ${docName}

## Project

- **Project**: ${context.projectName}

## Design Goals

- Explain the background and goal of ${docName}
- Record boundaries, constraints, and tradeoffs

## To Be Completed

- Background
- Proposal
- Risks
- Verification`, `# ${docName}

## プロジェクト

- **プロジェクト**: ${context.projectName}

## 設計目標

- ${docName} の背景と目的を説明する
- 境界、制約、トレードオフを記録する

## 未記入項目

- 背景
- 提案
- リスク
- 検証`, `# ${docName}

## المشروع

- **المشروع**: ${context.projectName}

## أهداف التصميم

- اشرح خلفية ${docName} وهدفه
- سجّل الحدود والقيود والمفاضلات

## عناصر يجب إكمالها

- الخلفية
- المقترح
- المخاطر
- التحقق`);
        return this.withFrontmatter({
            name: this.slugify(docName) || 'design-doc',
            title: this.copy(context.documentLanguage, `${docName} 设计文档`, `${docName} Design Doc`, `${docName} 設計文書`, `${docName} وثيقة تصميم`),
            tags: ['design', 'architecture', 'decision'],
        }, body);
    }
    generatePlanningDocTemplate(fallbackName, docName, input) {
        const context = this.getProjectContext(fallbackName, 'standard', input);
        const presetBody = this.getPresetPlanningDocBody(context, docName);
        const body = presetBody ?? this.copy(context.documentLanguage, `# ${docName}

## 项目

- **项目名称**：${context.projectName}

## 规划目标

- 说明 ${docName} 对应的阶段目标和交付边界

## 待补充内容

- 里程碑
- 任务拆解
- 风险与依赖
- 验收节奏`, `# ${docName}

## Project

- **Project**: ${context.projectName}

## Planning Goal

- Describe the phase goal and delivery boundary for ${docName}

## To Be Completed

- Milestones
- Task breakdown
- Risks and dependencies
- Acceptance cadence`, `# ${docName}

## プロジェクト

- **プロジェクト**: ${context.projectName}

## 計画目標

- ${docName} の段階目標と納品境界を記述する

## 未記入項目

- マイルストーン
- タスク分解
- リスクと依存関係
- 受け入れの進め方`, `# ${docName}

## المشروع

- **المشروع**: ${context.projectName}

## هدف التخطيط

- صف هدف المرحلة وحدود التسليم الخاصة بـ ${docName}

## عناصر يجب إكمالها

- المعالم
- تقسيم المهام
- المخاطر والاعتماديات
- إيقاع القبول`);
        return this.withFrontmatter({
            name: this.slugify(docName) || 'planning-doc',
            title: this.copy(context.documentLanguage, `${docName} 计划文档`, `${docName} Planning Doc`, `${docName} 計画文書`, `${docName} وثيقة تخطيط`),
            tags: ['planning', 'delivery', 'milestone'],
        }, body);
    }
    generateAiGuideTemplate(input) {
        const language = input?.documentLanguage === 'zh-CN' ||
            input?.documentLanguage === 'en-US' ||
            input?.documentLanguage === 'ja-JP' ||
            input?.documentLanguage === 'ar'
            ? input.documentLanguage
            : 'en-US';
        return this.withFrontmatter({
            name: 'ai-guide',
            title: this.copy(language, 'AI 开发指南', 'AI Guide', 'AI ガイド', 'دليل الذكاء الاصطناعي'),
            tags: ['ai', 'guide', 'workflow'],
        }, this.copy(language, `# AI 开发指南

## 工作顺序

1. 优先读取 \`SKILL.index.json\`。
2. 修改代码前先读取相关 \`SKILL.md\`。
3. 当前执行工作以 \`changes/active/<change>\` 为准。
4. 对 \`workflow_profile_id: change\`，只维护经典快速流程文件：\`proposal.md\`、\`tasks.md\`、\`state.json\`、\`verification.md\` 和 \`review.md\`；除非明确升级为 goal，不要创建 \`design.md\`、\`implementation-plan.md\`、task graph、worker packets 或 review artifacts。
4. 对 \`workflow_profile_id: goal\`，先基于需求、\`proposal.md\` 和项目上下文起草或更新 \`design.md\`，再编辑 \`implementation-plan.md\`、推导 \`artifacts/agents/task-graph.json\`、编辑 \`tasks.md\` 或代码。
5. \`Announce-Before-Act\`：绝不静默执行——宣告当前 skill 与阶段、即将运行的 \`ospec execute ...\` 命令与产物、派发多少原生 subagent 及机制（Claude Code 用 \`Task\`、Codex/GPT 用 \`spawn_agent\`/\`wait_agent\`/\`close_agent\`、Gemini 用 \`@generalist\`、OpenCode 用 \`@mention\`），以及被哪个门禁阻塞。
5. \`Brainstorm-First\`：goal 开局先做简短头脑风暴再锁定设计，把方向/架构/API/数据/UI/风险/范围的未决问题逐个问用户；优先升起持久 decision gate 而非静默假设，仅当用户明确让 AI 自行决定时才在 \`design.md\` 记录假设并标注待确认。
5. 当 change 必须等待用户选择后才能继续时，用 \`ospec execute decision [changes/active/<change>] --id <id> --question "..." --option id:label:影响 --option id:label:影响 [--recommended id] [--required]\` 写入持久 decision gate，向用户展示 decision report 的 \`Chat Prompt\` 或 \`artifacts/agents/decisions/index.md\`，再用 \`ospec execute decision [changes/active/<change>] --id <id> --select <option-id>\` 记录用户选择。
6. \`implementation-plan.md\` 必须从 \`design.md\` 推导，并明确目标文件、预期结果、验证命令、依赖、可并行任务和冲突；\`artifacts/agents/task-graph.json\` 必须从 \`implementation-plan.md\` 推导；\`tasks.md\` 必须从 task graph 推导。
6. 进入已有 OSpec 项目时，用 \`ospec session [path]\` 写入 \`.ospec/session-brief.json\` 和 \`.ospec/session-brief.md\`，记录 active change、queued change、queue-run、cache fingerprint 和安全下一步命令；这是项目入口简报，不替代 active change 的 \`ospec execute bootstrap\`。
6. 将已激活的内建质量策略步骤（如 \`tdd_cycle\`、\`root_cause_debug\`、\`verification_evidence\`）视为受归档门禁约束的 \`optional_steps\`；收尾前必须在任务文档、验证文档和对应 evidence artifacts 中覆盖。
6. 用 \`ospec execute status --brief\` 和当前 packet 定位下一步；再通过 \`SKILL.index.json\` 与 \`docs/project/feature-index.md\` 按需打开 artifacts 和文档。
7. 开始或恢复单个 active change 时，用 \`ospec execute bootstrap [changes/active/<change>]\` 写入带 project session brief snapshot 的 \`artifacts/agents/bootstrap.json\` 和 \`artifacts/agents/bootstrap.md\`，然后按其中的下一步安全动作继续；已有 active dispatch 时，bootstrap 会推荐对应的 \`ospec execute launch ... --task ...\` 命令。
7. 需要把下一条 OSpec 命令持久化给人或 AI 接手时，用 \`ospec execute route [changes/active/<change>]\` 写入 \`artifacts/agents/workflow-route.json\` 和 \`artifacts/agents/workflow-route.md\`；该命令只记录 workflow routing artifacts，不会编辑源码。
8. change 需要在 agent、工具、worktree、shell 或人工操作者之间交接时，用 \`ospec execute handoff [changes/active/<change>] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic]\` 写入 \`artifacts/agents/handoff.json\` 和 \`artifacts/agents/handoff.md\`；该命令只记录 project session brief snapshot、目标工具映射和安全规则，不会启动 worker 或编辑源码。
8. 推导或派发实现任务前，用 \`ospec execute doc-review [changes/active/<change>] [--stage design|plan]\` 生成带 project session brief snapshot 的 \`artifacts/agents/document-review-dispatches/\` 交接包，并创建 \`artifacts/reviews/design-review.md\` 或 \`artifacts/reviews/implementation-plan-review.md\`；design review 通过后才能派发 implementation plan review。
8. 派发 worker 前，用 \`ospec execute workspace [changes/active/<change>]\` 写入 \`artifacts/agents/workspace-status.json\` 和 \`artifacts/agents/workspace-status.md\`；如果状态为 \`needs_isolation\`，先清理当前工作区或转入隔离 git worktree，再进行并行派发。
9. 创建隔离 worktree 前，用 \`ospec execute worktree [changes/active/<change>] [--branch name] [--path path] [--base ref]\` 写入 \`artifacts/agents/worktree-plan.json\` 和 \`artifacts/agents/worktree-plan.md\`；该命令只记录准备计划，不会运行 \`git worktree add\`。
10. 最终收尾前，用 \`ospec execute finish [changes/active/<change>] [--target main] [--remote origin]\` 写入 \`artifacts/agents/finish-plan.json\` 和 \`artifacts/agents/finish-plan.md\`；该命令只记录 readiness 和命令文本，不会 finalize、archive、push、merge 或删除 worktree。当 finish plan 状态为 ready 且没有 required pending decision 时，继续运行 \`ospec finalize [changes/active/<change>]\`；\`ospec archive ... --check\` 只用于可选 dry-run 预览，检查通过后不要停在这里。
9. 需要 task 级持久交接时，用 \`ospec execute dispatch [changes/active/<change>] [--task task-id] [--limit N]\` 生成并行安全的 worker 任务包批次和 \`artifacts/agents/execution-session.json\`；每个 packet 都包含 project session brief snapshot 和 worker profile，说明 capability tier、recommended target、target tool mapping、rationale 和 required behavior；required pending user decision 会阻止 dispatch；用 \`ospec execute complete <task-id> ...\` 记录 worker 结果；用 \`--task\` 指定单个任务，用 \`--limit\` 限制派发批次大小；当 complete 记录 \`NEEDS_CONTEXT\` 或 \`BLOCKED\` 时，会生成 \`artifacts/agents/blockers/\`。
9. dispatch 后用 \`ospec execute launch [changes/active/<change>] [--task task-id] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic] [--dry-run] [--json]\` 写入 native agent 启动计划；它要求存在一个 active dispatch 且 workspace 状态为 ready，适配器需要 stdout 上的 machine-readable launch artifact 时使用 \`--json\`，并说明当前控制 AI 如何使用 harness 原生 agent 机制：Codex/GPT 用 \`spawn_agent\`、\`wait_agent\`、\`close_agent\`，Claude Code 用 Task，Gemini 用 \`@generalist\`，OpenCode 用 \`@mention\`，Cursor 用 Agent/task chat，Copilot 用 CLI/coding-agent task。
9. 默认多 worker 执行路径是当前 harness 原生 subagent：先 dispatch 并行安全 packet，查看 \`launch-plan.md\`，再为每个安全 packet 启动一个 native agent，并用 \`ospec execute complete\` 记录结果。
9. 只有当前 AI harness 不支持原生 subagent 时，才用 \`ospec execute orchestrate [changes/active/<change>] --command "..." [--limit N] [--max-rounds N] [--timeout-ms N]\` 作为最后 CLI fallback；fallback 会渲染显式 command template、并发运行 worker command、记录 \`artifacts/agents/orchestration-runs/\`，并把结果 collect 回 task graph，同时报告 failed-worker retry commands。
9. 只有原生 subagent 不可用或被明确绕过时，才用 \`--run --command\`（即 \`ospec execute launch ... --run --command "..."\`）作为单 worker CLI fallback，并写入 \`artifacts/agents/worker-runs/\`；随后用 \`ospec execute collect [changes/active/<change>] [--task task-id] [--run run-id]\` 记录 fallback 任务结果。修复 blocked、needs-context 或 failed work 后，用 \`ospec execute retry\` 重新派发；已完成任务必须显式 \`--force\` 才能 retry。
10. 每个 worker task 完成后，用 \`ospec execute review [changes/active/<change>] --task <task-id>\` 生成一个合并的 task code reviewer 交接包（一次同时审 spec 符合性与代码质量）；task 级决策写入 \`artifacts/reviews/tasks/<task-id>/review.md\`，依赖任务会等这一次合并 review 通过后才可派发。
10. 所有 task 级 review 通过且 task graph 完成后，用不带 \`--task\` 的 \`ospec execute review [changes/active/<change>]\` 在 \`artifacts/agents/review-dispatches/\` 下生成一个合并的最终整体 code reviewer 交接包；它产出单一 \`artifacts/reviews/final-review.md\` 决策。
10. 只有显式使用 \`ospec execute review ... --run --command "..."\` 时才运行本地 reviewer 命令，并写入 \`artifacts/agents/review-runs/\`；提供 \`--decision\` 时可写回 review decision。
11. review artifact 有非 \`PENDING\` 决策后，用 \`ospec execute feedback [changes/active/<change>] [--stage spec|quality]\` 写入 \`artifacts/agents/review-feedback-plan.json\` 和 \`artifacts/agents/review-feedback-plan.md\`；继续派发工作前要明确接受、修订、澄清或阻塞处理；当反馈改变范围、方向、API、UI、风险或已接受取舍时创建 required user decision。
11. 人工修改 task graph、execution session、review artifact、debug evidence 或 verification checklist 后，用 \`ospec execute sync [changes/active/<change>]\` 重建 \`artifacts/agents/worker-status.md\`。
12. \`ospec session\`、\`ospec execute bootstrap\`、\`handoff\`、\`doc-review\`、\`workspace\`、plan 模式 \`worktree\`、\`finish\`、\`dispatch\`、\`launch\`、\`collect\`、\`retry\`、\`complete\`、\`review\`、\`feedback\`、\`debug\`、\`tdd\`、\`verify\` 与 \`sync\` 只更新 OSpec artifacts；除 \`workspace\`、\`worktree\` 与 \`finish\` 会读取 git 状态外，不直接编辑项目 source files。原生 subagent 由当前 AI harness 启动；只有显式 \`worktree --create\`、\`worktree --cleanup\`、fallback \`launch --run --command\`、\`review --run --command\` 或 fallback \`orchestrate\` 才运行 shell 命令。
13. \`artifacts/agents/task-graph.json\` 中存在未解决 task 状态、无效依赖、缺失目标文件、缺失验证命令，或顶层 \`status\` 不是 \`completed\` 时，不得 archive。
14. 实现后必须完成每个 task 的一次合并 review；最终必须完成单一的 \`artifacts/reviews/final-review.md\`；任一 task 级或最终 review decision 仍为 \`PENDING\`、\`NEEDS_CHANGES\` 或 \`BLOCKED\` 时，不得 archive。
15. 实现和 review 阶段必须更新 \`artifacts/agents/worker-status.md\`；任一 worker 状态仍为 \`PENDING\`、\`NEEDS_CONTEXT\` 或 \`BLOCKED\` 时不得声称完成，归档前 \`controller_status\` 必须为 \`DONE\`。
16. 运行聚焦测试后，用 \`ospec execute tdd [changes/active/<change>] --phase red|green|refactor --command "..."\` 记录 \`artifacts/agents/tdd-evidence.json\`；该命令只记录 evidence，不会运行 shell 命令；red 不得为通过，green 必须有前置 red FAILED 记录，refactor 必须有前置通过的 green/refactor 证据，SKIPPED 必须写明具体原因。
17. 调试是 change 的一部分时，用 \`ospec execute debug [changes/active/<change>] --phase reproduce|isolate|hypothesize|fix|verify --symptom "..." --root-cause "..." --status FIXED\` 记录 \`artifacts/agents/debug-evidence.json\`；该命令只记录 evidence，不会运行 shell 命令。
18. 运行最新项目验证命令后，用 \`ospec execute verify [changes/active/<change>] --command "..." --status PASSED\` 记录 \`artifacts/agents/verification-evidence.json\`；不要只用聊天摘要声称完成。
19. 如果项目启用了 Stitch 且当前 change 激活了 \`stitch_design_review\`，先检查 \`artifacts/stitch/approval.json\`，未 \`approved\` 前不要把 change 视为可继续实现或可归档。
20. 如果涉及 Stitch 安装、provider 切换、doctor 修复、MCP 或认证配置，优先读取仓库内 Stitch 规范；若缺失规范文档，则使用内建基线：Gemini 用 \`%USERPROFILE%/.gemini/settings.json\` 的 \`mcpServers.stitch.httpUrl\` 与 \`headers.X-Goog-Api-Key\`，Codex 用 \`%USERPROFILE%/.codex/config.toml\` 的 \`[mcp_servers.stitch]\`、\`type = "http"\`、\`url = "https://stitch.googleapis.com/mcp"\` 与 \`X-Goog-Api-Key\`。
21. 如果内建 Codex provider 只读调用正常但 Stitch 写操作卡住，优先检查是否真正走了 \`codex exec --dangerously-bypass-approvals-and-sandbox\`；如果项目覆写了自定义 Codex runner，则该 runner 也必须显式带上这个参数。
22. 文档审查策略读取 \`.skillrc.workflow.document_review_policy\`：默认 \`always\` 保持独立 reviewer；\`adaptive\` 只有在目标文档显式声明 \`risk_level: low\`（或 \`none\`）且没有风险信号时才允许确定性 inline preflight，否则派独立 reviewer。
23. worker/reviewer 使用逻辑 model profile。命令执行器通过 \`OSPEC_USAGE_FILE\` 自动归集 usage，\`--usage-file\` 保留为手工入口；指标必须区分 complete、partial、missing，不能把未上报显示成零。
24. 最终 review 为 \`NEEDS_CHANGES\` 且 required decisions 已解决时，用 \`ospec execute repair [changes/active/<change>]\` 把完整 findings 合成一个 repair task，集中验证、task review 和 final re-review 各一次。
25. reviewer 同时写 Markdown 和相邻的 \`*.findings.json\`；声明的 \`documentation_updates\` 必须有有效内容变化证据，归档索引直接链接长期项目文档。
26. 每个归档的普通 change 和 goal 都自动生成并索引一份 \`docs/project/changes/<归档路径>.md\`；移动 active change 前，归档预检拒绝覆盖同路径人工文档并验证托管输出目录可写；这份摘要不能代替必须维护的架构、API、模块或运行文档。
27. 发生重要变更后同步更新技能文档并重建索引。`, `# AI Guide

## Working Order

1. Read \`SKILL.index.json\` when available.
2. Read the relevant \`SKILL.md\` files before editing code.
3. Use \`changes/active/<change>\` as the execution layer.
4. For \`workflow_profile_id: change\`, keep the classic fast-flow files only: \`proposal.md\`, \`tasks.md\`, \`state.json\`, \`verification.md\`, and \`review.md\`; do not create \`design.md\`, \`implementation-plan.md\`, task graphs, worker packets, or review artifacts unless the work is promoted to \`goal\`.
4. For \`workflow_profile_id: goal\`, draft or update \`design.md\` from the requirement, \`proposal.md\`, and project context before editing \`implementation-plan.md\`, deriving \`artifacts/agents/task-graph.json\`, editing \`tasks.md\`, or editing code.
5. \`Announce-Before-Act\`: never run the workflow silently — announce which OSpec skill and stage you are in, which \`ospec execute ...\` command you will run and the artifact it writes, how many native subagents you dispatch and via which mechanism (\`Task\` for Claude Code, \`spawn_agent\`/\`wait_agent\`/\`close_agent\` for Codex/GPT, \`@generalist\` for Gemini, \`@mention\` for OpenCode), and which gate is blocking when progress stops.
5. \`Brainstorm-First\`: open each goal with a short brainstorming pass before locking design; surface open questions for direction, architecture, API, data, UI, risk, and scope and ask the user one at a time; prefer raising a durable decision gate over a silent assumption, and only record an assumption in \`design.md\` when the user defers, labeled as an assumption to confirm.
5. When the change must pause for a user choice, record a durable gate with \`ospec execute decision [changes/active/<change>] --id <id> --question "..." --option id:label:impact --option id:label:impact [--recommended id] [--required]\`, present the decision report \`Chat Prompt\` or \`artifacts/agents/decisions/index.md\`, then record the selected option with \`ospec execute decision [changes/active/<change>] --id <id> --select <option-id>\`.
6. Derive \`implementation-plan.md\` from \`design.md\`, including target files, expected results, verification commands, dependencies, parallelizable work, and conflicts; derive \`artifacts/agents/task-graph.json\` from \`implementation-plan.md\`; derive \`tasks.md\` from the task graph.
6. When entering an existing OSpec project, use \`ospec session [path]\` to write \`.ospec/session-brief.json\` and \`.ospec/session-brief.md\` with active change, queued change, queue-run, cache fingerprint, and safe next command context; this project entry brief does not replace active-change \`ospec execute bootstrap\`.
6. Treat activated built-in quality policy steps such as \`tdd_cycle\`, \`root_cause_debug\`, and \`verification_evidence\` as archive-gated \`optional_steps\`; cover them in task documents, verification documents, and matching evidence artifacts before closeout.
6. Use \`ospec execute status --brief\` and the current packet to locate the next action, then open artifacts and docs on demand through \`SKILL.index.json\` and \`docs/project/feature-index.md\`.
7. When starting or resuming one active change, use \`ospec execute bootstrap [changes/active/<change>]\` to write \`artifacts/agents/bootstrap.json\` and \`artifacts/agents/bootstrap.md\` with the project session brief snapshot, then follow its next safe action; when an active dispatch is waiting, bootstrap recommends the matching \`ospec execute launch ... --task ...\` command.
7. When you need to persist the next recommended OSpec command for a human or AI handoff, use \`ospec execute route [changes/active/<change>]\` to write \`artifacts/agents/workflow-route.json\` and \`artifacts/agents/workflow-route.md\`; this records workflow routing artifacts only and does not edit source files.
8. When a change moves between agents, tools, worktrees, shells, or human operators, use \`ospec execute handoff [changes/active/<change>] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic]\` to write \`artifacts/agents/handoff.json\` and \`artifacts/agents/handoff.md\`; this records the project session brief snapshot, target tool mapping, and safety rules only and does not launch workers or edit source files.
8. Before deriving or dispatching implementation tasks, use \`ospec execute doc-review [changes/active/<change>] [--stage design|plan]\` to create document reviewer packets with the project session brief snapshot under \`artifacts/agents/document-review-dispatches/\` and review artifacts at \`artifacts/reviews/design-review.md\` or \`artifacts/reviews/implementation-plan-review.md\`; design review must be approved before implementation plan review.
8. Before worker handoff, use \`ospec execute workspace [changes/active/<change>]\` to write \`artifacts/agents/workspace-status.json\` and \`artifacts/agents/workspace-status.md\`; if status is \`needs_isolation\`, clean the workspace or move work into an isolated git worktree before parallel dispatch.
9. Before creating an isolated worktree, use \`ospec execute worktree [changes/active/<change>] [--branch name] [--path path] [--base ref]\` to write \`artifacts/agents/worktree-plan.json\` and \`artifacts/agents/worktree-plan.md\`; this command records a preparation plan only and does not run \`git worktree add\`.
10. Before final closeout, use \`ospec execute finish [changes/active/<change>] [--target main] [--remote origin]\` to write \`artifacts/agents/finish-plan.json\` and \`artifacts/agents/finish-plan.md\`; this command records readiness and command text only and does not finalize, archive, push, merge, or remove worktrees. When the finish plan status is ready and no required decision is pending, run \`ospec finalize [changes/active/<change>]\`; use \`ospec archive ... --check\` only as an optional dry-run preview and do not stop after it passes.
9. When task-level handoff needs durable artifacts, use \`ospec execute dispatch [changes/active/<change>] [--task task-id] [--limit N]\` to create a parallel-safe batch of worker packets and \`artifacts/agents/execution-session.json\`; each packet includes the project session brief snapshot and a worker profile with capability tier, recommended target, target tool mapping, rationale, and required behavior; required pending user decisions block dispatch; use \`ospec execute complete <task-id> ...\` to record worker results; use \`--task\` for one explicit task and \`--limit\` to cap dispatch batch size; \`complete\` writes \`artifacts/agents/blockers/\` when the result is \`NEEDS_CONTEXT\` or \`BLOCKED\`.
9. After dispatch, use \`ospec execute launch [changes/active/<change>] [--task task-id] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic] [--dry-run] [--json]\` to write the native agent launch plan; it requires one active dispatch and ready workspace status, supports \`--json\` for the machine-readable launch artifact, and tells the controlling AI how to use the current harness native mechanism: Codex/GPT \`spawn_agent\`/\`wait_agent\`/\`close_agent\`, Claude Code Task, Gemini \`@generalist\`, OpenCode \`@mention\`, Cursor Agent/task chat, and Copilot CLI/coding-agent task.
9. The default multi-worker path is current-harness native subagents: dispatch parallel-safe packets, inspect \`launch-plan.md\`, start one native agent per safe packet, and record every result with \`ospec execute complete\`.
9. Use \`ospec execute orchestrate [changes/active/<change>] --command "..." [--limit N] [--max-rounds N] [--timeout-ms N]\` only as the final CLI fallback when the current AI harness cannot dispatch native subagents; fallback renders an explicit command template, runs worker commands concurrently, records \`artifacts/agents/orchestration-runs/\`, collects results into the task graph, and reports failed-worker retry commands.
9. Use \`--run --command\` (that is, \`ospec execute launch ... --run --command "..."\`) only as single-worker CLI fallback when native subagents are unavailable or explicitly bypassed; it writes \`artifacts/agents/worker-runs/\`. Then use \`ospec execute collect [changes/active/<change>] [--task task-id] [--run run-id]\` to record the fallback task result. Use \`ospec execute retry\` to reopen corrected blocked, needs-context, or failed work; completed tasks require \`--force\`.
10. After each worker task completes, use \`ospec execute review [changes/active/<change>] --task <task-id>\` to create one combined task code reviewer packet (spec compliance and code quality in one pass); the task-level decision is stored at \`artifacts/reviews/tasks/<task-id>/review.md\`, and dependent tasks stay blocked until that one combined review is approved.
10. After all task-level reviews are approved and the task graph is completed, use \`ospec execute review [changes/active/<change>]\` without \`--task\` to create one combined final whole-change code reviewer packet with the project session brief snapshot under \`artifacts/agents/review-dispatches/\`; it writes a single \`artifacts/reviews/final-review.md\` decision.
10. Only explicit \`ospec execute review ... --run --command "..."\` runs a local reviewer command; it captures \`artifacts/agents/review-runs/\` and can write the review decision when \`--decision\` is provided.
11. After a review artifact has a non-\`PENDING\` decision, use \`ospec execute feedback [changes/active/<change>] [--stage spec|quality]\` to write \`artifacts/agents/review-feedback-plan.json\` and \`artifacts/agents/review-feedback-plan.md\`; handle feedback as accept, revise, clarify, or blocked before dispatching more work, and create a required user decision when feedback changes scope, direction, API, UI, risk, or accepted tradeoffs.
11. Use \`ospec execute sync [changes/active/<change>]\` after manual task graph, execution-session, review artifact, debug evidence, or verification checklist edits to rebuild \`artifacts/agents/worker-status.md\`.
12. \`ospec session\`, \`ospec execute bootstrap\`, \`handoff\`, \`doc-review\`, \`workspace\`, plan-mode \`worktree\`, \`finish\`, \`dispatch\`, \`launch\`, \`collect\`, \`retry\`, \`complete\`, \`review\`, \`feedback\`, \`debug\`, \`tdd\`, \`verify\`, and \`sync\` update OSpec artifacts only; except for \`workspace\`, \`worktree\`, and \`finish\` reading git state, they do not edit project source files directly. Native subagents are launched by the current AI harness; shell commands run only with explicit \`worktree --create\`, \`worktree --cleanup\`, fallback \`launch --run --command\`, \`review --run --command\`, or fallback \`orchestrate\`.
13. Do not archive while \`artifacts/agents/task-graph.json\` has unresolved task statuses, invalid dependencies, missing target files, missing verification commands, or top-level \`status\` other than \`completed\`.
14. After implementation, complete each task's one combined review, then complete the single final \`artifacts/reviews/final-review.md\`; do not archive while any task-level or final review decision is \`PENDING\`, \`NEEDS_CHANGES\`, or \`BLOCKED\`.
15. During implementation and review, update \`artifacts/agents/worker-status.md\`; do not claim completion while any worker status is \`PENDING\`, \`NEEDS_CONTEXT\`, or \`BLOCKED\`; \`controller_status\` must be \`DONE\` before archive.
16. After focused test runs, use \`ospec execute tdd [changes/active/<change>] --phase red|green|refactor --command "..."\` to record \`artifacts/agents/tdd-evidence.json\`; this command records evidence only and does not run shell commands; red must not pass, green requires a prior red FAILED record, refactor requires prior passing green/refactor evidence, and SKIPPED requires a concrete summary.
17. When debugging is part of the change, use \`ospec execute debug [changes/active/<change>] --phase reproduce|isolate|hypothesize|fix|verify --symptom "..." --root-cause "..." --status FIXED\` to record \`artifacts/agents/debug-evidence.json\`; this command records evidence only and does not run shell commands.
18. After running fresh project verification commands, use \`ospec execute verify [changes/active/<change>] --command "..." --status PASSED\` to record \`artifacts/agents/verification-evidence.json\`; do not claim completion with only a chat summary.
19. If Stitch is enabled and the current change activates \`stitch_design_review\`, inspect \`artifacts/stitch/approval.json\` first and do not treat the change as ready to continue or archive until it is \`approved\`.
20. If Stitch installation, provider switching, doctor remediation, MCP setup, or auth setup is involved, read \`.ospec/plugins/stitch/docs/\` first; if those docs are missing, install or enable Stitch to sync them before changing config.
21. If the built-in Codex provider succeeds on read-only calls but Stitch write operations stall, first verify the run actually uses \`codex exec --dangerously-bypass-approvals-and-sandbox\`; if the project overrides a custom Codex runner, that runner must also pass the same flag explicitly.
22. Read document review policy from \`.skillrc.workflow.document_review_policy\`: \`always\` is the compatible independent-review default; \`adaptive\` uses inline preflight only when the target document explicitly declares \`risk_level: low\` (or \`none\`) and no risk signal exists; otherwise dispatch an independent reviewer.
23. Workers and reviewers use logical model profiles. Command runners ingest \`OSPEC_USAGE_FILE\` automatically while \`--usage-file\` remains a manual input; metrics distinguish complete, partial, and missing coverage rather than treating absent counters as zero.
24. When final review is \`NEEDS_CHANGES\` and required decisions are resolved, use \`ospec execute repair [changes/active/<change>]\` to create one repair task from all findings, then run covering verification, one task review, and one final re-review.
25. Reviewers write Markdown plus sibling \`*.findings.json\`; declared \`documentation_updates\` require meaningful-change evidence and archive indexes link durable project documents directly.
26. Every archived classic change and goal automatically generates and indexes one \`docs/project/changes/<archive-path>.md\`; before moving the active change, archive preflight refuses to overwrite a human-owned file at that path and verifies managed output directories are writable; this summary does not replace required architecture, API, module, or operational docs.
27. Update skill documents and rebuild the index after meaningful changes.`, `# AI ガイド

## 作業順序

1. まず \`SKILL.index.json\` を読む。
2. コード編集前に関連する \`SKILL.md\` を読む。
3. 現在の作業は \`changes/active/<change>\` を基準に進める。
4. \`workflow_profile_id: change\` では、\`proposal.md\`、\`tasks.md\`、\`state.json\`、\`verification.md\`、\`review.md\` だけを維持する。goal に昇格しない限り、\`design.md\`、\`implementation-plan.md\`、task graph、worker packets、review artifacts は作成しない。
4. \`workflow_profile_id: goal\` では、要件、\`proposal.md\`、プロジェクト文脈から \`design.md\` を作成または更新してから \`implementation-plan.md\` を編集し、\`artifacts/agents/task-graph.json\` を導出し、\`tasks.md\` やコードを編集する。
5. \`Announce-Before-Act\`: ワークフローを黙って実行しない。どの skill・段階か、これから実行する \`ospec execute ...\` コマンドと生成物、何体の native subagent をどの機構（Claude Code は \`Task\`、Codex/GPT は \`spawn_agent\`/\`wait_agent\`/\`close_agent\`、Gemini は \`@generalist\`、OpenCode は \`@mention\`）で派遣するか、どのゲートでブロックしているかを伝える。
5. \`Brainstorm-First\`: 各 goal は設計確定前に短いブレインストーミングから始め、方向・アーキテクチャ・API・データ・UI・リスク・スコープの未決事項を 1 つずつ質問する。黙った仮定より durable な decision gate を優先し、ユーザーが委任した場合のみ \`design.md\` に要確認の仮定として記録する。
5. change がユーザー選択を待つ必要がある場合は \`ospec execute decision [changes/active/<change>] --id <id> --question "..." --option id:label:impact --option id:label:impact [--recommended id] [--required]\` で durable decision gate を記録し、decision report の \`Chat Prompt\` または \`artifacts/agents/decisions/index.md\` を提示してから、\`ospec execute decision [changes/active/<change>] --id <id> --select <option-id>\` で回答を記録する。
6. \`implementation-plan.md\` は \`design.md\` から導き、対象ファイル、期待結果、検証コマンド、依存関係、並行可能な作業、競合を記録する。\`artifacts/agents/task-graph.json\` は \`implementation-plan.md\` から導き、\`tasks.md\` は task graph から導く。
6. 既存の OSpec project に入るときは \`ospec session [path]\` で \`.ospec/session-brief.json\` と \`.ospec/session-brief.md\` を書き、active change、queued change、queue-run、cache fingerprint、次の安全な command context を記録する。これは project entry brief であり、active change の \`ospec execute bootstrap\` を置き換えない。
6. \`tdd_cycle\`、\`root_cause_debug\`、\`verification_evidence\` など有効化された built-in quality policy steps は、archive-gated \`optional_steps\` として扱う。closeout 前に task documents、verification documents、対応する evidence artifacts で coverage を記録する。
6. \`ospec execute status --brief\` と現在の packet で次の action を特定し、\`SKILL.index.json\` と \`docs/project/feature-index.md\` から必要な artifact と文書だけを開く。
7. one active change を開始または再開するときは、\`ospec execute bootstrap [changes/active/<change>]\` で project session brief snapshot を含む \`artifacts/agents/bootstrap.json\` と \`artifacts/agents/bootstrap.md\` を書き、そこにある次の安全な action に従う。active dispatch がある場合、bootstrap は対応する \`ospec execute launch ... --task ...\` command を推奨する。
7. 次に推奨される OSpec command を人や AI への handoff 用に残す必要がある場合、\`ospec execute route [changes/active/<change>]\` で \`artifacts/agents/workflow-route.json\` と \`artifacts/agents/workflow-route.md\` を記録する。この command は workflow routing artifacts だけを書き、source files は編集しない。
8. change を agent、tool、worktree、shell、human operator の間で引き渡すときは、\`ospec execute handoff [changes/active/<change>] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic]\` で \`artifacts/agents/handoff.json\` と \`artifacts/agents/handoff.md\` を書く。このコマンドは project session brief snapshot、target tool mapping、safety rules のみを記録し、worker 起動や source file 編集は行わない。
8. implementation tasks を導出または dispatch する前に、\`ospec execute doc-review [changes/active/<change>] [--stage design|plan]\` で \`artifacts/agents/document-review-dispatches/\` 配下に project session brief snapshot を含む document reviewer packet を作成し、\`artifacts/reviews/design-review.md\` または \`artifacts/reviews/implementation-plan-review.md\` を用意する。design review 承認後に implementation plan review を dispatch する。
8. worker handoff の前に \`ospec execute workspace [changes/active/<change>]\` で \`artifacts/agents/workspace-status.json\` と \`artifacts/agents/workspace-status.md\` を記録する。status が \`needs_isolation\` の場合は workspace を clean にするか isolated git worktree に移してから parallel dispatch する。
8. isolated worktree を作成する前に、\`ospec execute worktree [changes/active/<change>] [--branch name] [--path path] [--base ref]\` で \`artifacts/agents/worktree-plan.json\` と \`artifacts/agents/worktree-plan.md\` を記録する。このコマンドは準備計画のみを記録し、\`git worktree add\` は実行しない。
9. final closeout の前に、\`ospec execute finish [changes/active/<change>] [--target main] [--remote origin]\` で \`artifacts/agents/finish-plan.json\` と \`artifacts/agents/finish-plan.md\` を記録する。このコマンドは readiness と command text のみを記録し、finalize、archive、push、merge、worktree 削除は実行しない。finish plan の status が ready で required pending decision がない場合は、続けて \`ospec finalize [changes/active/<change>]\` を実行する。\`ospec archive ... --check\` は任意の dry-run preview のみで、通過後にそこで止めない。
9. task-level handoff に durable artifacts が必要な場合は、\`ospec execute dispatch [changes/active/<change>] [--task task-id] [--limit N]\` で parallel-safe worker packet batch と \`artifacts/agents/execution-session.json\` を作成する。各 packet には project session brief snapshot と、capability tier、recommended target、target tool mapping、rationale、required behavior を示す worker profile が含まれる。\`ospec execute complete <task-id> ...\` で worker result を記録する。\`--task\` は明示的な単一 task、\`--limit\` は dispatch batch size の上限に使う。\`complete\` が \`NEEDS_CONTEXT\` または \`BLOCKED\` を記録した場合は \`artifacts/agents/blockers/\` が生成される。
9. dispatch 後は \`ospec execute launch [changes/active/<change>] [--task task-id] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic] [--dry-run] [--json]\` で native agent launch plan を書く。active dispatch と ready workspace status が必要で、adapter が stdout の machine-readable launch artifact を必要とする場合は \`--json\` を使い、Codex/GPT は \`spawn_agent\`/\`wait_agent\`/\`close_agent\`、Claude Code は Task、Gemini は \`@generalist\`、OpenCode は \`@mention\`、Cursor は Agent/task chat、Copilot は CLI/coding-agent task を使う指示を記録する。
9. default の multi-worker path は current harness の native subagent である。parallel-safe packet を dispatch し、\`launch-plan.md\` を確認し、安全な packet ごとに native agent を起動し、\`ospec execute complete\` で結果を記録する。
9. current AI harness が native subagent を使えない場合だけ、\`ospec execute orchestrate [changes/active/<change>] --command "..." [--limit N] [--max-rounds N] [--timeout-ms N]\` を最後の CLI fallback として使う。fallback は explicit command template を render し、worker command を並行実行し、\`artifacts/agents/orchestration-runs/\` を記録して task graph に collect し、failed-worker retry commands を報告する。
9. native subagent が使えない、または明示的に bypass された場合だけ、single-worker CLI fallback として \`--run --command\`（つまり \`ospec execute launch ... --run --command "..."\`）を使い、\`artifacts/agents/worker-runs/\` を記録する。その後 \`ospec execute collect [changes/active/<change>] [--task task-id] [--run run-id]\` で fallback task result を記録する。blocked/needs-context/failed work の修正後は \`ospec execute retry\` で再 dispatch する。完了済み task は \`--force\` が必要。
10. 各 worker task 完了後、\`ospec execute review [changes/active/<change>] --task <task-id>\` で統合 task code reviewer packet（spec compliance と code quality を一度に確認）を 1 つ作成する。task-level review decision は \`artifacts/reviews/tasks/<task-id>/review.md\` に保存され、その 1 回の統合 review が承認されるまで dependent task は dispatch されない。
10. すべての task-level review が承認され task graph が完了した後、\`--task\` なしの \`ospec execute review [changes/active/<change>]\` で \`artifacts/agents/review-dispatches/\` 配下に 1 つの統合 final whole-change code reviewer packet を作成する。これは単一 \`artifacts/reviews/final-review.md\` の decision を書く。
10. explicit \`ospec execute review ... --run --command "..."\` の場合のみ local reviewer command を実行し、\`artifacts/agents/review-runs/\` を記録する。\`--decision\` がある場合は review decision も書き戻せる。
11. review artifact が non-\`PENDING\` decision を持つ場合は \`ospec execute feedback [changes/active/<change>] [--stage spec|quality]\` で \`artifacts/agents/review-feedback-plan.json\` と \`artifacts/agents/review-feedback-plan.md\` を書く。追加作業を dispatch する前に accept、revise、clarify、blocked の handling を明確にし、feedback が scope、direction、API、UI、risk、accepted tradeoffs を変える場合は required user decision を作成する。
11. task graph、execution session、review artifact、debug evidence、verification checklist を手動編集した後は \`ospec execute sync [changes/active/<change>]\` で \`artifacts/agents/worker-status.md\` を再構築する。
12. \`ospec session\`、\`ospec execute bootstrap\`、\`handoff\`、\`doc-review\`、\`workspace\`、plan-mode \`worktree\`、\`finish\`、\`dispatch\`、\`launch\`、\`collect\`、\`retry\`、\`complete\`、\`review\`、\`feedback\`、\`debug\`、\`tdd\`、\`verify\`、\`sync\` は OSpec artifacts のみを更新する。\`workspace\`、\`worktree\`、\`finish\` が git state を読む場合を除き、project source files は直接編集しない。native subagent は current AI harness が起動する。shell command は explicit \`worktree --create\`、\`worktree --cleanup\`、fallback \`launch --run --command\`、\`review --run --command\`、または fallback \`orchestrate\` の場合のみ実行する。
13. \`artifacts/agents/task-graph.json\` に未解決の task 状態、無効な依存関係、対象ファイル不足、検証コマンド不足、またはトップレベル \`status\` が \`completed\` でない状態がある場合は archive しない。
14. 実装後は各 task の 1 回の統合 review を完了し、単一の final \`artifacts/reviews/final-review.md\` を完了する。task-level または final review decision が \`PENDING\`、\`NEEDS_CHANGES\`、\`BLOCKED\` のままなら archive しない。
15. 実装と review の間は \`artifacts/agents/worker-status.md\` を更新する。worker 状態が \`PENDING\`、\`NEEDS_CONTEXT\`、\`BLOCKED\` のままなら完了を主張しない。archive 前に \`controller_status\` は \`DONE\` でなければならない。
16. focused test 実行後、\`ospec execute tdd [changes/active/<change>] --phase red|green|refactor --command "..."\` で \`artifacts/agents/tdd-evidence.json\` を記録する。このコマンドは evidence の記録のみを行い、shell command は実行しない。red は passing にできず、green には prior red FAILED record が必要で、refactor には prior passing green/refactor evidence が必要。SKIPPED には具体的な summary が必要。
17. debugging が change の一部だった場合、\`ospec execute debug [changes/active/<change>] --phase reproduce|isolate|hypothesize|fix|verify --symptom "..." --root-cause "..." --status FIXED\` で \`artifacts/agents/debug-evidence.json\` を記録する。このコマンドは evidence の記録のみを行い、shell command は実行しない。
18. fresh project verification commands を実行した後、\`ospec execute verify [changes/active/<change>] --command "..." --status PASSED\` で \`artifacts/agents/verification-evidence.json\` を記録する。chat summary だけで完了を主張しない。
19. Stitch が有効で、現在の change が \`stitch_design_review\` を有効化している場合は、先に \`artifacts/stitch/approval.json\` を確認し、\`approved\` になるまで継続や archive 可と見なさない。
20. Stitch のインストール、provider 切り替え、doctor 修復、MCP 設定、認証設定が関係する場合は、まずリポジトリ内の Stitch 仕様を読む。仕様がない場合のみ内蔵ベースラインを使う。
21. 内蔵 Codex provider が読み取り専用呼び出しでは成功するのに Stitch 書き込みが止まる場合は、\`codex exec --dangerously-bypass-approvals-and-sandbox\` が本当に使われているかを確認する。カスタム Codex runner を使う場合も同じフラグを明示的に渡す。
22. document review policy は \`.skillrc.workflow.document_review_policy\` から読み、既定の \`always\` は独立 review を維持する。\`adaptive\` は対象文書が \`risk_level: low\`（または \`none\`）を明示し risk signal がない場合だけ inline preflight を許可し、それ以外は独立 reviewer を使う。
23. worker/reviewer は logical model profile を使う。command runner は \`OSPEC_USAGE_FILE\` を自動集計し、\`--usage-file\` は手動入力として残す。metrics は complete、partial、missing を区別し、未報告値を 0 と扱わない。
24. final review が \`NEEDS_CHANGES\` なら、required decisions 解決後に \`ospec execute repair [changes/active/<change>]\` で全 findings を 1 repair task にまとめ、verification、task review、final re-review を各 1 回行う。
25. reviewer は Markdown と隣接 \`*.findings.json\` を書く。宣言済み \`documentation_updates\` には有意な変更 evidence が必要で、archive index は永続 project document を直接リンクする。
26. archive 済み classic change と goal ごとに \`docs/project/changes/<archive-path>.md\` を自動生成して index する。active change を移動する前に、archive preflight は同じ path にある人所有の文書の上書きを拒否し、管理対象の出力ディレクトリが書き込み可能であることを確認する。この概要は必要な architecture、API、module、operation 文書の代わりにはならない。
27. 重要な変更後は SKILL 文書を更新し、インデックスを再生成する。`, `# دليل الذكاء الاصطناعي

## ترتيب العمل

1. اقرأ \`SKILL.index.json\` أولاً إن وجد.
2. اقرأ ملفات \`SKILL.md\` ذات الصلة قبل تعديل الكود.
3. استخدم \`changes/active/<change>\` كطبقة التنفيذ الحالية.
4. عند \`workflow_profile_id: change\`، حافظ فقط على ملفات المسار السريع: \`proposal.md\` و\`tasks.md\` و\`state.json\` و\`verification.md\` و\`review.md\`؛ ولا تنشئ \`design.md\` أو \`implementation-plan.md\` أو task graph أو worker packets أو review artifacts إلا إذا ترقى العمل إلى \`goal\`.
4. عند \`workflow_profile_id: goal\`، أنشئ \`design.md\` أو حدّثه من المتطلب و\`proposal.md\` وسياق المشروع قبل تعديل \`implementation-plan.md\` أو اشتقاق \`artifacts/agents/task-graph.json\` أو تعديل \`tasks.md\` أو الكود.
5. \`Announce-Before-Act\`: لا تُشغّل سير العمل بصمت — أعلن أي skill ومرحلة، وأي أمر \`ospec execute ...\` ستشغّله وما يكتبه، وكم subagent أصلي ستوزّع وبأي آلية (\`Task\` لـ Claude Code، و\`spawn_agent\`/\`wait_agent\`/\`close_agent\` لـ Codex/GPT، و\`@generalist\` لـ Gemini، و\`@mention\` لـ OpenCode)، وأي بوابة تحجب التقدم.
5. \`Brainstorm-First\`: ابدأ كل goal بعصف ذهني قصير قبل تثبيت التصميم، واسأل المستخدم عن الاتجاه والبنية وAPI والبيانات وUI والمخاطر والنطاق واحداً تلو الآخر؛ وفضّل رفع decision gate دائمة على الافتراض الصامت، ولا تسجّل افتراضاً في \`design.md\` إلا عند تفويض المستخدم مع وسمه كافتراض بحاجة لتأكيد.
5. عندما يجب أن ينتظر change اختيار المستخدم، سجّل durable decision gate عبر \`ospec execute decision [changes/active/<change>] --id <id> --question "..." --option id:label:impact --option id:label:impact [--recommended id] [--required]\`، واعرض \`Chat Prompt\` من decision report أو \`artifacts/agents/decisions/index.md\`، ثم سجّل الإجابة عبر \`ospec execute decision [changes/active/<change>] --id <id> --select <option-id>\`.
6. يجب اشتقاق \`implementation-plan.md\` من \`design.md\` مع الملفات المستهدفة والنتائج المتوقعة وأوامر التحقق والاعتماديات والعمل القابل للتوازي والتعارضات؛ ويجب اشتقاق \`artifacts/agents/task-graph.json\` من \`implementation-plan.md\`؛ ويجب اشتقاق \`tasks.md\` من task graph.
6. عند الدخول إلى مشروع OSpec موجود، استخدم \`ospec session [path]\` لكتابة \`.ospec/session-brief.json\` و\`.ospec/session-brief.md\` مع سياق active change وqueued change وqueue-run وcache fingerprint والأمر الآمن التالي؛ هذا project entry brief ولا يستبدل \`ospec execute bootstrap\` للـ active change.
6. تعامل مع خطوات built-in quality policy المفعّلة مثل \`tdd_cycle\` و\`root_cause_debug\` و\`verification_evidence\` كـ \`optional_steps\` خاضعة لـ archive gate؛ غطّها في task documents وverification documents وملفات evidence المطابقة قبل closeout.
6. استخدم \`ospec execute status --brief\` وpacket الحالي لتحديد الإجراء التالي، ثم افتح artifacts والوثائق المطلوبة فقط عبر \`SKILL.index.json\` و\`docs/project/feature-index.md\`.
7. عند بدء أو استئناف active change واحد، استخدم \`ospec execute bootstrap [changes/active/<change>]\` لكتابة \`artifacts/agents/bootstrap.json\` و\`artifacts/agents/bootstrap.md\` مع project session brief snapshot، ثم اتبع الإجراء الآمن التالي المسجل فيه؛ عند وجود active dispatch، يوصي bootstrap بأمر \`ospec execute launch ... --task ...\` المطابق.
7. عندما تحتاج إلى حفظ أمر OSpec التالي الموصى به لتسليمه إلى إنسان أو AI، استخدم \`ospec execute route [changes/active/<change>]\` لكتابة \`artifacts/agents/workflow-route.json\` و\`artifacts/agents/workflow-route.md\`؛ هذا يسجل workflow routing artifacts فقط ولا يعدل source files.
8. عند نقل change بين agents أو tools أو worktrees أو shells أو operators بشريين، استخدم \`ospec execute handoff [changes/active/<change>] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic]\` لكتابة \`artifacts/agents/handoff.json\` و\`artifacts/agents/handoff.md\`؛ يسجل هذا الأمر project session brief snapshot وtool mapping وقواعد السلامة فقط ولا يشغّل workers أو يعدّل source files.
8. قبل اشتقاق implementation tasks أو dispatch لها، استخدم \`ospec execute doc-review [changes/active/<change>] [--stage design|plan]\` لإنشاء document reviewer packets تتضمن project session brief snapshot تحت \`artifacts/agents/document-review-dispatches/\` وتجهيز \`artifacts/reviews/design-review.md\` أو \`artifacts/reviews/implementation-plan-review.md\`؛ يجب اعتماد design review قبل dispatch لمراجعة implementation plan.
8. قبل handoff إلى worker، استخدم \`ospec execute workspace [changes/active/<change>]\` لكتابة \`artifacts/agents/workspace-status.json\` و\`artifacts/agents/workspace-status.md\`؛ إذا كانت الحالة \`needs_isolation\` فنظّف workspace أو انقل العمل إلى git worktree معزول قبل parallel dispatch.
8. قبل إنشاء git worktree معزول، استخدم \`ospec execute worktree [changes/active/<change>] [--branch name] [--path path] [--base ref]\` لكتابة \`artifacts/agents/worktree-plan.json\` و\`artifacts/agents/worktree-plan.md\`؛ هذا الأمر يسجل خطة تحضير فقط ولا يشغّل \`git worktree add\`.
9. قبل الإغلاق النهائي، استخدم \`ospec execute finish [changes/active/<change>] [--target main] [--remote origin]\` لكتابة \`artifacts/agents/finish-plan.json\` و\`artifacts/agents/finish-plan.md\`؛ هذا الأمر يسجل الجاهزية ونص الأوامر فقط ولا يشغّل finalize أو archive أو push أو merge أو حذف worktree. عندما تكون حالة finish plan هي ready ولا توجد required pending decision، شغّل \`ospec finalize [changes/active/<change>]\`؛ استخدم \`ospec archive ... --check\` كمعاينة dry-run اختيارية فقط ولا تتوقف بعد نجاحها.
9. عندما تحتاج task-level handoff إلى durable artifacts، استخدم \`ospec execute dispatch [changes/active/<change>] [--task task-id] [--limit N]\` لإنشاء batch آمن للتوازي من worker packets و\`artifacts/agents/execution-session.json\`؛ يتضمن كل packet project session brief snapshot وworker profile يوضح capability tier وrecommended target وtarget tool mapping وrationale وrequired behavior؛ واستخدم \`ospec execute complete <task-id> ...\` لتسجيل worker results؛ استخدم \`--task\` لمهمة واحدة صريحة و\`--limit\` لتحديد حجم dispatch batch؛ وعندما يسجل \`complete\` النتيجة \`NEEDS_CONTEXT\` أو \`BLOCKED\` يتم إنشاء \`artifacts/agents/blockers/\`.
9. بعد dispatch، استخدم \`ospec execute launch [changes/active/<change>] [--task task-id] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic] [--dry-run] [--json]\` لكتابة native agent launch plan؛ يتطلب active dispatch واحدا وحالة workspace ready، واستخدم \`--json\` عندما يحتاج adapter إلى machine-readable launch artifact على stdout، ويوضح للـ AI المتحكم آلية harness الأصلية: Codex/GPT عبر \`spawn_agent\`/\`wait_agent\`/\`close_agent\`، وClaude Code عبر Task، وGemini عبر \`@generalist\`، وOpenCode عبر \`@mention\`، وCursor عبر Agent/task chat، وCopilot عبر CLI/coding-agent task.
9. مسار multi-worker الافتراضي هو native subagents في current harness: أنشئ parallel-safe packets، وافحص \`launch-plan.md\`، ثم شغّل native agent واحدا لكل packet آمن، وسجل كل نتيجة عبر \`ospec execute complete\`.
9. استخدم \`ospec execute orchestrate [changes/active/<change>] --command "..." [--limit N] [--max-rounds N] [--timeout-ms N]\` فقط كآخر CLI fallback عندما لا يدعم current AI harness native subagents؛ يرندر fallback explicit command template، ويشغّل worker commands بالتوازي، ويسجل \`artifacts/agents/orchestration-runs/\`، ثم يجمع النتائج إلى task graph ويعرض failed-worker retry commands.
9. استخدم \`--run --command\` (أي \`ospec execute launch ... --run --command "..."\`) فقط كـ single-worker CLI fallback عندما لا تتوفر native subagents أو يتم تجاوزها صراحة؛ يسجل ذلك \`artifacts/agents/worker-runs/\`. بعدها استخدم \`ospec execute collect [changes/active/<change>] [--task task-id] [--run run-id]\` لتسجيل fallback task result. بعد إصلاح blocked أو needs-context أو failed work استخدم \`ospec execute retry\` لإعادة dispatch؛ المهام المكتملة تحتاج \`--force\`.
10. بعد اكتمال كل worker task، استخدم \`ospec execute review [changes/active/<change>] --task <task-id>\` لإنشاء حزمة code reviewer موحدة واحدة (spec compliance وcode quality في تمريرة واحدة)؛ يحفظ قرار task-level في \`artifacts/reviews/tasks/<task-id>/review.md\` وتبقى المهام التابعة محجوبة حتى اعتماد تلك المراجعة الموحدة الواحدة.
10. بعد اعتماد كل task-level reviews واكتمال task graph، استخدم \`ospec execute review [changes/active/<change>]\` من دون \`--task\` لإنشاء حزمة code reviewer نهائية موحدة واحدة تتضمن project session brief snapshot تحت \`artifacts/agents/review-dispatches/\`؛ تكتب قرار \`artifacts/reviews/final-review.md\` واحدا.
10. لا يشغّل OSpec local reviewer command إلا عند استخدام \`ospec execute review ... --run --command "..."\` صراحة؛ يسجل ذلك \`artifacts/agents/review-runs/\` ويمكنه كتابة review decision عند تمرير \`--decision\`.
11. بعد أن يحتوي review artifact على قرار غير \`PENDING\`، استخدم \`ospec execute feedback [changes/active/<change>] [--stage spec|quality]\` لكتابة \`artifacts/agents/review-feedback-plan.json\` و\`artifacts/agents/review-feedback-plan.md\`؛ حدد accept أو revise أو clarify أو blocked قبل dispatch عمل إضافي، وأنشئ required user decision عندما يغير feedback scope أو direction أو API أو UI أو risk أو accepted tradeoffs.
11. بعد تعديل task graph أو execution session أو review artifact أو debug evidence أو verification checklist يدويا، استخدم \`ospec execute sync [changes/active/<change>]\` لإعادة بناء \`artifacts/agents/worker-status.md\`.
12. \`ospec session\` و\`ospec execute bootstrap\` و\`handoff\` و\`doc-review\` و\`workspace\` وplan-mode \`worktree\` و\`finish\` و\`dispatch\` و\`launch\` و\`collect\` و\`retry\` و\`complete\` و\`review\` و\`feedback\` و\`debug\` و\`tdd\` و\`verify\` و\`sync\` تحدّث OSpec artifacts فقط؛ وباستثناء قراءة \`workspace\` و\`worktree\` و\`finish\` لحالة git، لا تعدّل project source files مباشرة. يتم تشغيل native subagents بواسطة current AI harness؛ ولا تعمل أوامر shell إلا مع explicit \`worktree --create\` أو \`worktree --cleanup\` أو fallback \`launch --run --command\` أو \`review --run --command\` أو fallback \`orchestrate\`.
13. لا تؤرشف عندما يحتوي \`artifacts/agents/task-graph.json\` على حالات مهام غير محسومة أو اعتماديات غير صالحة أو ملفات مستهدفة ناقصة أو أوامر تحقق ناقصة أو عندما لا يكون \`status\` العلوي \`completed\`.
14. بعد التنفيذ، أكمل المراجعة الموحدة الواحدة لكل task، ثم أكمل \`artifacts/reviews/final-review.md\` النهائية الموحدة؛ ولا تؤرشف ما دام أي task-level أو final review decision هو \`PENDING\` أو \`NEEDS_CHANGES\` أو \`BLOCKED\`.
15. أثناء التنفيذ والمراجعة، حدّث \`artifacts/agents/worker-status.md\`؛ ولا تدّعِ الاكتمال ما دامت أي حالة worker هي \`PENDING\` أو \`NEEDS_CONTEXT\` أو \`BLOCKED\`؛ ويجب أن تكون \`controller_status\` هي \`DONE\` قبل الأرشفة.
16. بعد تشغيل focused tests، استخدم \`ospec execute tdd [changes/active/<change>] --phase red|green|refactor --command "..."\` لتسجيل \`artifacts/agents/tdd-evidence.json\`؛ هذا الأمر يسجل evidence فقط ولا يشغّل أوامر shell؛ يجب ألا يكون red ناجحا، ويتطلب green سجلا سابقا red FAILED، ويتطلب refactor دليلا سابقا green/refactor ناجحا، ويتطلب SKIPPED ملخصا محددا.
17. عندما يكون debugging جزءا من change، استخدم \`ospec execute debug [changes/active/<change>] --phase reproduce|isolate|hypothesize|fix|verify --symptom "..." --root-cause "..." --status FIXED\` لتسجيل \`artifacts/agents/debug-evidence.json\`؛ هذا الأمر يسجل evidence فقط ولا يشغّل أوامر shell.
18. بعد تشغيل project verification commands حديثة، استخدم \`ospec execute verify [changes/active/<change>] --command "..." --status PASSED\` لتسجيل \`artifacts/agents/verification-evidence.json\`؛ ولا تدّعِ الاكتمال بمجرد ملخص داخل المحادثة.
19. إذا كان Stitch مفعلاً وكان change الحالي يفعّل \`stitch_design_review\`، فافحص \`artifacts/stitch/approval.json\` أولاً ولا تعتبر change جاهزاً للاستمرار أو الأرشفة حتى تصبح حالته \`approved\`.
20. إذا كان العمل يتضمن تثبيت Stitch أو تبديل provider أو إصلاح doctor أو إعداد MCP أو المصادقة، فاقرأ مواصفة Stitch المحلية في المستودع أولاً. استخدم الخطوط الأساسية المدمجة فقط عند غياب المواصفة.
21. إذا نجح provider الداخلي لـ Codex في الاستدعاءات للقراءة فقط لكن توقفت عمليات الكتابة الخاصة بـ Stitch، فتحقق أولاً من أن التشغيل يستخدم فعلاً \`codex exec --dangerously-bypass-approvals-and-sandbox\`. وإذا كان المشروع يبدل Runner مخصصاً لـ Codex فيجب أن يمرر العلم نفسه صراحةً.
22. اقرأ document review policy من \`.skillrc.workflow.document_review_policy\`؛ يحافظ \`always\` على reviewer مستقل، ولا يسمح \`adaptive\` بـ inline preflight إلا عندما تصرح الوثيقة بـ \`risk_level: low\` (أو \`none\`) ولا توجد risk signals؛ وإلا يوزع reviewer مستقلا.
23. يستخدم worker/reviewer logical model profile. يجمع command runner \`OSPEC_USAGE_FILE\` تلقائيا ويبقى \`--usage-file\` للإدخال اليدوي؛ وتفرق metrics بين complete وpartial وmissing ولا تعتبر القيمة الغائبة صفرا.
24. عندما يكون final review هو \`NEEDS_CHANGES\` وبعد حل required decisions، استخدم \`ospec execute repair [changes/active/<change>]\` لإنشاء repair task واحدة من كل findings ثم verification وtask review وfinal re-review مرة واحدة لكل منها.
25. يكتب reviewer ملف Markdown وملف \`*.findings.json\` مجاورا؛ وتتطلب \`documentation_updates\` دليلا على تغير فعلي، ويربط archive index وثائق المشروع الدائمة مباشرة.
26. ينشئ كل classic change وgoal مؤرشف وثيقة \`docs/project/changes/<archive-path>.md\` ويفهرسها تلقائيا؛ وقبل نقل change النشط يرفض فحص archive المسبق الكتابة فوق وثيقة يملكها البشر في المسار نفسه ويتحقق من قابلية الكتابة في أدلة الإخراج المُدارة؛ ولا يغني هذا الملخص عن وثائق architecture أو API أو module أو operation المطلوبة.
27. حدّث وثائق SKILL وأعد بناء الفهرس بعد التغييرات المهمة.`));
    }
    generateExecutionProtocolTemplate(input) {
        const language = input?.documentLanguage === 'zh-CN' ||
            input?.documentLanguage === 'en-US' ||
            input?.documentLanguage === 'ja-JP' ||
            input?.documentLanguage === 'ar'
            ? input.documentLanguage
            : 'en-US';
        return this.withFrontmatter({
            name: 'execution-protocol',
            title: this.copy(language, '执行协议', 'Execution Protocol', '実行プロトコル', 'بروتوكول التنفيذ'),
            tags: ['execution', 'protocol', 'workflow'],
        }, this.copy(language, `# 执行协议

## 真正来源

- \`.skillrc\`
- \`.ospec/session-brief.md\`
- \`SKILL.index.json\` 与 \`docs/project/feature-index.md\`
- 当前 \`ospec execute status --brief\` 输出或 dispatch/review/decision/verification packet

先把 brief 或 packet 当作任务上下文，再按索引打开当前阶段需要的 change artifacts、目标文件和项目/归档文档。不要默认加载全部 goal artifacts。

## 规则

项目级文档和分层技能文档用于承载长期知识。
active change 文档用于承载当前实现工作。
\`workflow_profile_id: change\` 使用经典快速流程，只要求 \`proposal.md\`、\`tasks.md\`、\`state.json\`、\`verification.md\` 和 \`review.md\`；不要为了补齐模板而生成 goal-only 文档。
\`workflow_profile_id: goal\` 使用完整流程，才要求 \`design.md\`、\`implementation-plan.md\`、\`artifacts/agents/task-graph.json\`、document reviews、final reviews、worker status 和 verification evidence。
进入已有 OSpec 项目时，用 \`ospec session [path]\` 写入 \`.ospec/session-brief.json\` 和 \`.ospec/session-brief.md\`；该项目入口简报只记录 active change、queued change、queue-run、cache fingerprint 和安全下一步命令，不替代 active change 的 \`ospec execute bootstrap\`。
将已激活的内建质量策略步骤（如 \`tdd_cycle\`、\`root_cause_debug\`、\`verification_evidence\`）视为受归档门禁约束的 \`optional_steps\`；收尾前必须在任务文档、验证文档和对应 evidence artifacts 中覆盖。
AI 辅助执行 goal 时，先基于需求、\`proposal.md\` 和项目上下文起草或更新 \`design.md\`，再编辑 \`implementation-plan.md\`、推导 \`artifacts/agents/task-graph.json\`、编辑 \`tasks.md\` 或代码。
\`Announce-Before-Act\`：绝不静默执行——宣告当前 skill 与阶段、即将运行的 \`ospec execute ...\` 命令与产物、派发多少原生 subagent 及机制（Claude Code 用 \`Task\`、Codex/GPT 用 \`spawn_agent\`/\`wait_agent\`/\`close_agent\`、Gemini 用 \`@generalist\`、OpenCode 用 \`@mention\`），以及被哪个门禁阻塞。
\`Brainstorm-First\`：goal 开局先做简短头脑风暴再锁定设计，把方向/架构/API/数据/UI/风险/范围的未决问题逐个问用户；优先升起持久 decision gate 而非静默假设，仅当用户明确让 AI 自行决定时才在 \`design.md\` 记录假设并标注待确认。
当 change 必须等待用户选择后才能继续时，用 \`ospec execute decision [changes/active/<change>] --id <id> --question "..." --option id:label:影响 --option id:label:影响 [--recommended id] [--required]\` 写入持久 decision gate，向用户展示 decision report 的 \`Chat Prompt\` 或 \`artifacts/agents/decisions/index.md\`，再用 \`ospec execute decision [changes/active/<change>] --id <id> --select <option-id>\` 记录用户选择。
\`implementation-plan.md\` 必须从 \`design.md\` 推导，并明确目标文件、预期结果、验证命令、依赖、可并行任务和冲突；\`artifacts/agents/task-graph.json\` 必须从 \`implementation-plan.md\` 推导；\`tasks.md\` 必须从 task graph 推导。
开始或恢复单个 active change 时，用 \`ospec execute bootstrap [changes/active/<change>]\` 写入带 project session brief snapshot 的 \`artifacts/agents/bootstrap.json\` 和 \`artifacts/agents/bootstrap.md\`，然后按其中的下一步安全动作继续；已有 active dispatch 时，bootstrap 会推荐对应的 \`ospec execute launch ... --task ...\` 命令。
需要把下一条 OSpec 命令持久化给人或 AI 接手时，用 \`ospec execute route [changes/active/<change>]\` 写入 \`artifacts/agents/workflow-route.json\` 和 \`artifacts/agents/workflow-route.md\`；该命令只记录 workflow routing artifacts，不会编辑源码。
change 需要在 agent、工具、worktree、shell 或人工操作者之间交接时，用 \`ospec execute handoff [changes/active/<change>] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic]\` 写入 \`artifacts/agents/handoff.json\` 和 \`artifacts/agents/handoff.md\`；该命令只记录 project session brief snapshot、目标工具映射和安全规则，不会启动 worker 或编辑源码。
推导或派发实现任务前，用 \`ospec execute doc-review [changes/active/<change>] [--stage design|plan]\` 生成带 project session brief snapshot 的 \`artifacts/agents/document-review-dispatches/\` 交接包，并创建 \`artifacts/reviews/design-review.md\` 或 \`artifacts/reviews/implementation-plan-review.md\`；design review 通过后才能派发 implementation plan review。
派发 worker 前，用 \`ospec execute workspace [changes/active/<change>]\` 写入 \`artifacts/agents/workspace-status.json\` 和 \`artifacts/agents/workspace-status.md\`；如果状态为 \`needs_isolation\`，先清理当前工作区或转入隔离 git worktree，再进行并行派发。
创建隔离 worktree 前，用 \`ospec execute worktree [changes/active/<change>] [--branch name] [--path path] [--base ref]\` 写入 \`artifacts/agents/worktree-plan.json\` 和 \`artifacts/agents/worktree-plan.md\`；该命令只记录准备计划，不会运行 \`git worktree add\`。
最终收尾前，用 \`ospec execute finish [changes/active/<change>] [--target main] [--remote origin]\` 写入 \`artifacts/agents/finish-plan.json\` 和 \`artifacts/agents/finish-plan.md\`；该命令只记录 readiness 和命令文本，不会 finalize、archive、push、merge 或删除 worktree。当 finish plan 状态为 ready 且没有 required pending decision 时，继续运行 \`ospec finalize [changes/active/<change>]\`；\`ospec archive ... --check\` 只用于可选 dry-run 预览，检查通过后不要停在这里。
用 \`ospec execute dispatch [changes/active/<change>] [--task task-id] [--limit N]\` 生成并行安全的 worker 任务包批次和 \`artifacts/agents/execution-session.json\`；每个 packet 都包含 project session brief snapshot 和 worker profile，说明 capability tier、recommended target、target tool mapping、rationale 和 required behavior；required pending user decision 会阻止 dispatch；用 \`ospec execute complete <task-id> ...\` 记录 worker 结果；用 \`--task\` 指定单个任务，用 \`--limit\` 限制派发批次大小；当 complete 记录 \`NEEDS_CONTEXT\` 或 \`BLOCKED\` 时，会生成 \`artifacts/agents/blockers/\`。
dispatch 后用 \`ospec execute launch [changes/active/<change>] [--task task-id] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic] [--dry-run] [--json]\` 写入 native agent 启动计划；它要求存在一个 active dispatch 且 workspace 状态为 ready，适配器需要 stdout 上的 machine-readable launch artifact 时使用 \`--json\`，并说明当前控制 AI 如何使用 harness 原生 agent 机制：Codex/GPT 用 \`spawn_agent\`、\`wait_agent\`、\`close_agent\`，Claude Code 用 Task，Gemini 用 \`@generalist\`，OpenCode 用 \`@mention\`，Cursor 用 Agent/task chat，Copilot 用 CLI/coding-agent task。
默认多 worker 执行路径是当前 harness 原生 subagent：先 dispatch 并行安全 packet，查看 \`launch-plan.md\`，再为每个安全 packet 启动一个 native agent，并用 \`ospec execute complete\` 记录结果。
只有当前 AI harness 不支持原生 subagent 时，才用 \`ospec execute orchestrate [changes/active/<change>] --command "..." [--limit N] [--max-rounds N] [--timeout-ms N]\` 作为最后 CLI fallback；fallback 会渲染显式 command template、并发运行 worker command、记录 \`artifacts/agents/orchestration-runs/\`，并把结果 collect 回 task graph，同时报告 failed-worker retry commands。
只有原生 subagent 不可用或被明确绕过时，才用 \`--run --command\`（即 \`ospec execute launch ... --run --command "..."\`）作为单 worker CLI fallback，并写入 \`artifacts/agents/worker-runs/\`；随后用 \`ospec execute collect [changes/active/<change>] [--task task-id] [--run run-id]\` 记录 fallback 任务结果。修复 blocked、needs-context 或 failed work 后，用 \`ospec execute retry\` 重新派发；已完成任务必须显式 \`--force\` 才能 retry。
每个 worker task 完成后，用 \`ospec execute review [changes/active/<change>] --task <task-id>\` 生成一个合并的 task code reviewer 交接包（一次同时审 spec 符合性与代码质量）；task 级决策写入 \`artifacts/reviews/tasks/<task-id>/review.md\`，依赖任务会等这一次合并 review 通过后才可派发。
所有 task 级 review 通过且 task graph 完成后，用不带 \`--task\` 的 \`ospec execute review [changes/active/<change>]\` 在 \`artifacts/agents/review-dispatches/\` 下生成一个合并的最终整体 code reviewer 交接包；它产出单一 \`artifacts/reviews/final-review.md\` 决策。
只有显式使用 \`ospec execute review ... --run --command "..."\` 时才运行本地 reviewer 命令，并写入 \`artifacts/agents/review-runs/\`；提供 \`--decision\` 时可写回 review decision。
review artifact 有非 \`PENDING\` 决策后，用 \`ospec execute feedback [changes/active/<change>] [--stage spec|quality]\` 写入 \`artifacts/agents/review-feedback-plan.json\` 和 \`artifacts/agents/review-feedback-plan.md\`；继续派发工作前要明确接受、修订、澄清或阻塞处理；当反馈改变范围、方向、API、UI、风险或已接受取舍时创建 required user decision。
人工修改 task graph、execution session、review artifact、debug evidence 或 verification checklist 后，用 \`ospec execute sync [changes/active/<change>]\` 重建 \`artifacts/agents/worker-status.md\`。
\`ospec session\`、\`ospec execute bootstrap\`、\`handoff\`、\`doc-review\`、\`workspace\`、plan 模式 \`worktree\`、\`finish\`、\`dispatch\`、\`launch\`、\`collect\`、\`retry\`、\`complete\`、\`review\`、\`feedback\`、\`debug\`、\`tdd\`、\`verify\` 与 \`sync\` 只更新 OSpec artifacts；除 \`workspace\`、\`worktree\` 与 \`finish\` 会读取 git 状态外，不直接编辑项目 source files。原生 subagent 由当前 AI harness 启动；只有显式 \`worktree --create\`、\`worktree --cleanup\`、fallback \`launch --run --command\`、\`review --run --command\` 或 fallback \`orchestrate\` 才运行 shell 命令。
\`artifacts/agents/task-graph.json\` 中存在未解决 task 状态、无效依赖、缺失执行细节，或顶层 \`status\` 不是 \`completed\` 时，不得 archive。
实现和 review 阶段必须更新 \`artifacts/agents/worker-status.md\`；任一 worker 状态仍为 \`PENDING\`、\`NEEDS_CONTEXT\` 或 \`BLOCKED\` 时，不得声称完成。
归档前 \`controller_status\` 必须为 \`DONE\`。
运行聚焦测试后，必须用 \`ospec execute tdd [changes/active/<change>] --phase red|green|refactor --command "..."\` 记录 \`artifacts/agents/tdd-evidence.json\`，或记录不适用原因。
调试是 change 的一部分时，必须用 \`ospec execute debug [changes/active/<change>] --phase reproduce|isolate|hypothesize|fix|verify --symptom "..." --root-cause "..." --status FIXED\` 记录 \`artifacts/agents/debug-evidence.json\`，或记录不适用原因。
运行最新项目验证命令后，必须用 \`ospec execute verify [changes/active/<change>] --command "..." --status PASSED\` 记录 \`artifacts/agents/verification-evidence.json\`。
实现后必须完成每个 task 的一次合并 review；最终必须完成单一的 \`artifacts/reviews/final-review.md\`；任一 task 级或最终 review decision 仍为 \`PENDING\`、\`NEEDS_CHANGES\` 或 \`BLOCKED\` 时，不得 archive。
\`.skillrc.workflow.document_review_policy\` 默认是 \`always\`；\`adaptive\` 只有在目标文档显式声明 \`risk_level: low\`（或 \`none\`）且没有风险信号时内联批准，否则仍派独立 reviewer。
worker/reviewer packet 必须记录逻辑 model profile、解析到的 model 或 harness-default 回退；命令执行器自动归集 \`OSPEC_USAGE_FILE\`，并报告 complete/partial/missing 覆盖率。
reviewer 必须同时写 Markdown 与相邻的 \`*.findings.json\`；声明的文档更新必须有有效内容变化证据，归档索引必须链接长期项目文档。
每个归档的普通 change 和 goal 必须自动生成并索引 \`docs/project/changes/<归档路径>.md\`；移动 active change 前，预检必须拒绝覆盖同路径人工文档并验证托管输出目录可写；后置检查必须确认 change 文档和两个索引链接都存在。
最终 review 为 \`NEEDS_CHANGES\` 时，解决 required decision 后用 \`ospec execute repair\` 把全部 findings 合成一个 repair task，再各运行一次 covering verification、task review 和 final re-review。

## 插件阻断

- 先读取 \`.skillrc.plugins\`，判断是否存在启用中的阻断插件。
- 如果当前 change 在 \`tasks.md\` 或 \`verification.md\` 中激活了 \`stitch_design_review\`，先检查 \`artifacts/stitch/approval.json\`。
- 当 \`approval.json.status\` 不是 \`approved\` 时，不要把 change 视为可继续实现、可验证完成或可归档。
- 如果涉及 Stitch provider、MCP 或认证配置，优先读取仓库内 Stitch 规范；若缺失规范文档，则使用内建基线：Gemini 用 \`%USERPROFILE%/.gemini/settings.json\` 的 \`mcpServers.stitch.httpUrl\` 与 \`headers.X-Goog-Api-Key\`，Codex 用 \`%USERPROFILE%/.codex/config.toml\` 的 \`[mcp_servers.stitch]\`、\`type = "http"\`、\`url = "https://stitch.googleapis.com/mcp"\` 与 \`X-Goog-Api-Key\`。
- 如果内建 Codex provider 只读调用正常但 Stitch 写操作卡住，优先检查是否真正走了 \`codex exec --dangerously-bypass-approvals-and-sandbox\`；如果项目覆写了自定义 Codex runner，则该 runner 也必须显式带上这个参数。
- 记录人工审批时，优先使用 \`ospec plugins approve stitch <change-path>\` 或 \`ospec plugins reject stitch <change-path>\`。`, `# Execution Protocol

## Source of Truth

- \`.skillrc\`
- \`.ospec/session-brief.md\`
- \`SKILL.index.json\` and \`docs/project/feature-index.md\`
- the current \`ospec execute status --brief\` output or dispatch/review/decision/verification packet

Treat the brief or packet as task context, then use the indexes to open only the change artifacts, target files, and project/archive docs required by the current stage. Do not load every goal artifact by default.

## Rule

Use project-level docs and layered skill files for long-lived knowledge.
Use active changes for current implementation work.
For \`workflow_profile_id: change\`, use the classic fast flow and require only \`proposal.md\`, \`tasks.md\`, \`state.json\`, \`verification.md\`, and \`review.md\`; do not backfill goal-only documents just because a template mentions them.
For \`workflow_profile_id: goal\`, use the full workflow and require \`design.md\`, \`implementation-plan.md\`, \`artifacts/agents/task-graph.json\`, document reviews, final reviews, worker status, and verification evidence.
When entering an existing OSpec project, use \`ospec session [path]\` to write \`.ospec/session-brief.json\` and \`.ospec/session-brief.md\`; this project entry brief records active change, queued change, queue-run, cache fingerprint, and safe next command context only and does not replace active-change \`ospec execute bootstrap\`.
Treat activated built-in quality policy steps such as \`tdd_cycle\`, \`root_cause_debug\`, and \`verification_evidence\` as archive-gated \`optional_steps\`; cover them in task documents, verification documents, and matching evidence artifacts before closeout.
During AI-assisted goal execution, draft or update \`design.md\` from the requirement, \`proposal.md\`, and project context before editing \`implementation-plan.md\`, deriving \`artifacts/agents/task-graph.json\`, editing \`tasks.md\`, or editing code.
\`Announce-Before-Act\`: never run the workflow silently — announce which OSpec skill and stage you are in, which \`ospec execute ...\` command you will run and the artifact it writes, how many native subagents you dispatch and via which mechanism (\`Task\` for Claude Code, \`spawn_agent\`/\`wait_agent\`/\`close_agent\` for Codex/GPT, \`@generalist\` for Gemini, \`@mention\` for OpenCode), and which gate is blocking when progress stops.
\`Brainstorm-First\`: open each goal with a short brainstorming pass before locking design; surface open questions for direction, architecture, API, data, UI, risk, and scope and ask the user one at a time; prefer raising a durable decision gate over a silent assumption, and only record an assumption in \`design.md\` when the user defers, labeled as an assumption to confirm.
When the change must pause for a user choice, record a durable gate with \`ospec execute decision [changes/active/<change>] --id <id> --question "..." --option id:label:impact --option id:label:impact [--recommended id] [--required]\`, present the decision report \`Chat Prompt\` or \`artifacts/agents/decisions/index.md\`, then record the selected option with \`ospec execute decision [changes/active/<change>] --id <id> --select <option-id>\`.
Derive \`implementation-plan.md\` from \`design.md\`, including target files, expected results, verification commands, dependencies, parallelizable work, and conflicts; derive \`artifacts/agents/task-graph.json\` from \`implementation-plan.md\`; derive \`tasks.md\` from the task graph.
When starting or resuming one active change, use \`ospec execute bootstrap [changes/active/<change>]\` to write \`artifacts/agents/bootstrap.json\` and \`artifacts/agents/bootstrap.md\` with the project session brief snapshot, then follow its next safe action; when an active dispatch is waiting, bootstrap recommends the matching \`ospec execute launch ... --task ...\` command.
When you need to persist the next recommended OSpec command for a human or AI handoff, use \`ospec execute route [changes/active/<change>]\` to write \`artifacts/agents/workflow-route.json\` and \`artifacts/agents/workflow-route.md\`; this records workflow routing artifacts only and does not edit source files.
When a change moves between agents, tools, worktrees, shells, or human operators, use \`ospec execute handoff [changes/active/<change>] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic]\` to write \`artifacts/agents/handoff.json\` and \`artifacts/agents/handoff.md\`; this records the project session brief snapshot, target tool mapping, and safety rules only and does not launch workers or edit source files.
Before deriving or dispatching implementation tasks, use \`ospec execute doc-review [changes/active/<change>] [--stage design|plan]\` to create document reviewer packets with the project session brief snapshot under \`artifacts/agents/document-review-dispatches/\` and review artifacts at \`artifacts/reviews/design-review.md\` or \`artifacts/reviews/implementation-plan-review.md\`; design review must be approved before implementation plan review.
Before worker handoff, use \`ospec execute workspace [changes/active/<change>]\` to write \`artifacts/agents/workspace-status.json\` and \`artifacts/agents/workspace-status.md\`; if status is \`needs_isolation\`, clean the workspace or move work into an isolated git worktree before parallel dispatch.
Before creating an isolated worktree, use \`ospec execute worktree [changes/active/<change>] [--branch name] [--path path] [--base ref]\` to write \`artifacts/agents/worktree-plan.json\` and \`artifacts/agents/worktree-plan.md\`; this command records a preparation plan only and does not run \`git worktree add\`.
Before final closeout, use \`ospec execute finish [changes/active/<change>] [--target main] [--remote origin]\` to write \`artifacts/agents/finish-plan.json\` and \`artifacts/agents/finish-plan.md\`; this command records readiness and command text only and does not finalize, archive, push, merge, or remove worktrees. When the finish plan status is ready and no required decision is pending, run \`ospec finalize [changes/active/<change>]\`; use \`ospec archive ... --check\` only as an optional dry-run preview and do not stop after it passes.
Use \`ospec execute dispatch [changes/active/<change>] [--task task-id] [--limit N]\` to create a parallel-safe batch of worker packets and \`artifacts/agents/execution-session.json\`; each packet includes the project session brief snapshot and a worker profile with capability tier, recommended target, target tool mapping, rationale, and required behavior; required pending user decisions block dispatch; use \`ospec execute complete <task-id> ...\` to record worker results; use \`--task\` for one explicit task and \`--limit\` to cap dispatch batch size; \`complete\` writes \`artifacts/agents/blockers/\` when the result is \`NEEDS_CONTEXT\` or \`BLOCKED\`.
After dispatch, use \`ospec execute launch [changes/active/<change>] [--task task-id] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic] [--dry-run] [--json]\` to write the native agent launch plan; it requires one active dispatch and ready workspace status, supports \`--json\` for the machine-readable launch artifact, and tells the controlling AI how to use the current harness native mechanism: Codex/GPT \`spawn_agent\`/\`wait_agent\`/\`close_agent\`, Claude Code Task, Gemini \`@generalist\`, OpenCode \`@mention\`, Cursor Agent/task chat, and Copilot CLI/coding-agent task.
The default multi-worker path is current-harness native subagents: dispatch parallel-safe packets, inspect \`launch-plan.md\`, start one native agent per safe packet, and record every result with \`ospec execute complete\`.
Use \`ospec execute orchestrate [changes/active/<change>] --command "..." [--limit N] [--max-rounds N] [--timeout-ms N]\` only as the final CLI fallback when the current AI harness cannot dispatch native subagents; fallback renders an explicit command template, runs worker commands concurrently, records \`artifacts/agents/orchestration-runs/\`, collects results into the task graph, and reports failed-worker retry commands.
Use \`--run --command\` (that is, \`ospec execute launch ... --run --command "..."\`) only as single-worker CLI fallback when native subagents are unavailable or explicitly bypassed; it writes \`artifacts/agents/worker-runs/\`. Then use \`ospec execute collect [changes/active/<change>] [--task task-id] [--run run-id]\` to record the fallback task result. Use \`ospec execute retry\` to reopen corrected blocked, needs-context, or failed work; completed tasks require \`--force\`.
After each worker task completes, use \`ospec execute review [changes/active/<change>] --task <task-id>\` to create one combined task code reviewer packet (spec compliance and code quality in one pass); the task-level decision is stored at \`artifacts/reviews/tasks/<task-id>/review.md\`, and dependent tasks stay blocked until that one combined review is approved.
After all task-level reviews are approved and the task graph is completed, use \`ospec execute review [changes/active/<change>]\` without \`--task\` to create one combined final whole-change code reviewer packet with the project session brief snapshot under \`artifacts/agents/review-dispatches/\`; it writes a single \`artifacts/reviews/final-review.md\` decision.
Only explicit \`ospec execute review ... --run --command "..."\` runs a local reviewer command; it captures \`artifacts/agents/review-runs/\` and can write the review decision when \`--decision\` is provided.
After a review artifact has a non-\`PENDING\` decision, use \`ospec execute feedback [changes/active/<change>] [--stage spec|quality]\` to write \`artifacts/agents/review-feedback-plan.json\` and \`artifacts/agents/review-feedback-plan.md\`; handle feedback as accept, revise, clarify, or blocked before dispatching more work, and create a required user decision when feedback changes scope, direction, API, UI, risk, or accepted tradeoffs.
Use \`ospec execute sync [changes/active/<change>]\` after manual task graph, execution-session, review artifact, debug evidence, or verification checklist edits to rebuild \`artifacts/agents/worker-status.md\`.
\`ospec session\`, \`ospec execute bootstrap\`, \`handoff\`, \`doc-review\`, \`workspace\`, plan-mode \`worktree\`, \`finish\`, \`dispatch\`, \`launch\`, \`collect\`, \`retry\`, \`complete\`, \`review\`, \`feedback\`, \`debug\`, \`tdd\`, \`verify\`, and \`sync\` update OSpec artifacts only; except for \`workspace\`, \`worktree\`, and \`finish\` reading git state, they do not edit project source files directly. Native subagents are launched by the current AI harness; shell commands run only with explicit \`worktree --create\`, \`worktree --cleanup\`, fallback \`launch --run --command\`, \`review --run --command\`, or fallback \`orchestrate\`.
Do not archive while \`artifacts/agents/task-graph.json\` has unresolved task statuses, invalid dependencies, missing execution details, or top-level \`status\` other than \`completed\`.
During implementation and review, update \`artifacts/agents/worker-status.md\`; do not claim completion while any worker status is \`PENDING\`, \`NEEDS_CONTEXT\`, or \`BLOCKED\`.
\`controller_status\` must be \`DONE\` before archive.
After focused test runs, record \`artifacts/agents/tdd-evidence.json\` with \`ospec execute tdd [changes/active/<change>] --phase red|green|refactor --command "..."\`, or record why it is not applicable.
When debugging is part of the change, record \`artifacts/agents/debug-evidence.json\` with \`ospec execute debug [changes/active/<change>] --phase reproduce|isolate|hypothesize|fix|verify --symptom "..." --root-cause "..." --status FIXED\`, or record why it is not applicable.
After running fresh project verification commands, record \`artifacts/agents/verification-evidence.json\` with \`ospec execute verify [changes/active/<change>] --command "..." --status PASSED\`.
After implementation, complete each task's one combined review, then complete the single final \`artifacts/reviews/final-review.md\`; do not archive while any task-level or final review decision is \`PENDING\`, \`NEEDS_CHANGES\`, or \`BLOCKED\`.
\`.skillrc.workflow.document_review_policy\` defaults to \`always\`; \`adaptive\` approves inline only when the target document explicitly declares \`risk_level: low\` (or \`none\`) and no risk signal exists, otherwise it dispatches an independent reviewer.
Worker/reviewer packets record the logical model profile, configured model, or explicit harness-default fallback; command runners ingest \`OSPEC_USAGE_FILE\` automatically and report complete/partial/missing coverage.
Reviewers write Markdown and sibling \`*.findings.json\`; declared documentation updates require meaningful-change evidence and archive indexes link durable project documents.
Every archived classic change and goal must generate and index \`docs/project/changes/<archive-path>.md\`; before moving the active change, preflight must refuse to overwrite a human-owned file at that path and verify managed output directories are writable; the postcondition verifies the document and both index links.
When final review is \`NEEDS_CHANGES\`, resolve required decisions and use \`ospec execute repair\` to group all findings into one repair task, followed by one covering verification, one task review, and one final re-review.

## Plugin Gates

- Read \`.skillrc.plugins\` first to detect enabled blocking plugins.
- If the current change activates \`stitch_design_review\` in \`tasks.md\` or \`verification.md\`, inspect \`artifacts/stitch/approval.json\` before advancing the change.
- When \`approval.json.status\` is not \`approved\`, do not treat the change as ready for continued implementation, completion claims, or archive readiness.
- If Stitch provider, MCP, or auth config is involved, read \`.ospec/plugins/stitch/docs/\` first; if those docs are missing, install or enable Stitch to sync them before changing config.
- If the built-in Codex provider succeeds on read-only calls but Stitch write operations stall, first verify the run actually uses \`codex exec --dangerously-bypass-approvals-and-sandbox\`; if the project overrides a custom Codex runner, that runner must also pass the same flag explicitly.
- When recording manual review decisions, prefer \`ospec plugins approve stitch <change-path>\` or \`ospec plugins reject stitch <change-path>\`.`, `# 実行プロトコル

## 参照元

- \`.skillrc\`
- \`.ospec/session-brief.md\`
- \`SKILL.index.json\` と \`docs/project/feature-index.md\`
- 現在の \`ospec execute status --brief\` 出力、または dispatch/review/decision/verification packet

brief または packet をタスク文脈として扱い、索引から現在の段階に必要な change artifact、対象ファイル、project/archive 文書だけを開く。すべての goal artifact を既定で読み込まない。

## ルール

長期知識にはプロジェクト文書とレイヤー化された SKILL を使う。
現在の実装作業には active change を使う。
\`workflow_profile_id: change\` では classic fast flow を使い、\`proposal.md\`、\`tasks.md\`、\`state.json\`、\`verification.md\`、\`review.md\` だけを要求する。テンプレートに記載があっても goal-only 文書を補完しない。
\`workflow_profile_id: goal\` では full workflow を使い、\`design.md\`、\`implementation-plan.md\`、\`artifacts/agents/task-graph.json\`、document reviews、final reviews、worker status、verification evidence を要求する。
既存の OSpec project に入るときは \`ospec session [path]\` で \`.ospec/session-brief.json\` と \`.ospec/session-brief.md\` を書く。この project entry brief は active change、queued change、queue-run、cache fingerprint、次の安全な command context のみを記録し、active change の \`ospec execute bootstrap\` を置き換えない。
\`tdd_cycle\`、\`root_cause_debug\`、\`verification_evidence\` など有効化された built-in quality policy steps は、archive-gated \`optional_steps\` として扱う。closeout 前に task documents、verification documents、対応する evidence artifacts で coverage を記録する。
AI 支援で goal を進める場合は、要件、\`proposal.md\`、プロジェクト文脈から \`design.md\` を作成または更新してから \`implementation-plan.md\` を編集し、\`artifacts/agents/task-graph.json\` を導出し、\`tasks.md\` やコードを編集する。
\`Announce-Before-Act\`: ワークフローを黙って実行しない。どの skill・段階か、これから実行する \`ospec execute ...\` コマンドと生成物、何体の native subagent をどの機構（Claude Code は \`Task\`、Codex/GPT は \`spawn_agent\`/\`wait_agent\`/\`close_agent\`、Gemini は \`@generalist\`、OpenCode は \`@mention\`）で派遣するか、どのゲートでブロックしているかを伝える。
\`Brainstorm-First\`: 各 goal は設計確定前に短いブレインストーミングから始め、方向・アーキテクチャ・API・データ・UI・リスク・スコープの未決事項を 1 つずつ質問する。黙った仮定より durable な decision gate を優先し、ユーザーが委任した場合のみ \`design.md\` に要確認の仮定として記録する。
change がユーザー選択を待つ必要がある場合は \`ospec execute decision [changes/active/<change>] --id <id> --question "..." --option id:label:impact --option id:label:impact [--recommended id] [--required]\` で durable decision gate を記録し、decision report の \`Chat Prompt\` または \`artifacts/agents/decisions/index.md\` を提示してから、\`ospec execute decision [changes/active/<change>] --id <id> --select <option-id>\` で回答を記録する。
\`implementation-plan.md\` は \`design.md\` から導き、対象ファイル、期待結果、検証コマンド、依存関係、並行可能な作業、競合を記録する。\`artifacts/agents/task-graph.json\` は \`implementation-plan.md\` から導き、\`tasks.md\` は task graph から導く。
one active change を開始または再開するときは、\`ospec execute bootstrap [changes/active/<change>]\` で project session brief snapshot を含む \`artifacts/agents/bootstrap.json\` と \`artifacts/agents/bootstrap.md\` を書き、そこにある次の安全な action に従う。active dispatch がある場合、bootstrap は対応する \`ospec execute launch ... --task ...\` command を推奨する。
次に推奨される OSpec command を人や AI への handoff 用に残す必要がある場合、\`ospec execute route [changes/active/<change>]\` で \`artifacts/agents/workflow-route.json\` と \`artifacts/agents/workflow-route.md\` を記録する。この command は workflow routing artifacts だけを書き、source files は編集しない。
change を agent、tool、worktree、shell、human operator の間で引き渡すときは、\`ospec execute handoff [changes/active/<change>] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic]\` で \`artifacts/agents/handoff.json\` と \`artifacts/agents/handoff.md\` を書く。このコマンドは project session brief snapshot、target tool mapping、safety rules のみを記録し、worker 起動や source file 編集は行わない。
implementation tasks を導出または dispatch する前に、\`ospec execute doc-review [changes/active/<change>] [--stage design|plan]\` で \`artifacts/agents/document-review-dispatches/\` 配下に project session brief snapshot を含む document reviewer packet を作成し、\`artifacts/reviews/design-review.md\` または \`artifacts/reviews/implementation-plan-review.md\` を用意する。design review 承認後に implementation plan review を dispatch する。
worker handoff の前に \`ospec execute workspace [changes/active/<change>]\` で \`artifacts/agents/workspace-status.json\` と \`artifacts/agents/workspace-status.md\` を記録する。status が \`needs_isolation\` の場合は workspace を clean にするか isolated git worktree に移してから parallel dispatch する。
isolated worktree を作成する前に、\`ospec execute worktree [changes/active/<change>] [--branch name] [--path path] [--base ref]\` で \`artifacts/agents/worktree-plan.json\` と \`artifacts/agents/worktree-plan.md\` を記録する。このコマンドは準備計画のみを記録し、\`git worktree add\` は実行しない。
final closeout の前に、\`ospec execute finish [changes/active/<change>] [--target main] [--remote origin]\` で \`artifacts/agents/finish-plan.json\` と \`artifacts/agents/finish-plan.md\` を記録する。このコマンドは readiness と command text のみを記録し、finalize、archive、push、merge、worktree 削除は実行しない。finish plan の status が ready で required pending decision がない場合は、続けて \`ospec finalize [changes/active/<change>]\` を実行する。\`ospec archive ... --check\` は任意の dry-run preview のみで、通過後にそこで止めない。
\`ospec execute dispatch [changes/active/<change>] [--task task-id] [--limit N]\` で parallel-safe worker packet batch と \`artifacts/agents/execution-session.json\` を作成する。各 packet には project session brief snapshot と、capability tier、recommended target、target tool mapping、rationale、required behavior を示す worker profile が含まれる。\`ospec execute complete <task-id> ...\` で worker result を記録する。\`--task\` は明示的な単一 task、\`--limit\` は dispatch batch size の上限に使う。\`complete\` が \`NEEDS_CONTEXT\` または \`BLOCKED\` を記録した場合は \`artifacts/agents/blockers/\` が生成される。
dispatch 後は \`ospec execute launch [changes/active/<change>] [--task task-id] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic] [--dry-run] [--json]\` で native agent launch plan を書く。active dispatch と ready workspace status が必要で、adapter が stdout の machine-readable launch artifact を必要とする場合は \`--json\` を使い、Codex/GPT は \`spawn_agent\`/\`wait_agent\`/\`close_agent\`、Claude Code は Task、Gemini は \`@generalist\`、OpenCode は \`@mention\`、Cursor は Agent/task chat、Copilot は CLI/coding-agent task を使う指示を記録する。
multi-worker execution は current harness native subagents が default。parallel-safe packet を dispatch し、\`artifacts/agents/launch-plan.md\` を確認し、安全な packet ごとに native agent を起動し、\`ospec execute complete\` で結果を記録する。
current AI harness が native subagent を使えない場合だけ、\`ospec execute orchestrate [changes/active/<change>] --command "..." [--limit N] [--max-rounds N] [--timeout-ms N]\` を最後の CLI fallback として使う。fallback は explicit command template を render し、worker command を並行実行し、\`artifacts/agents/orchestration-runs/\` を記録して task graph に collect し、failed-worker retry commands を報告する。
native subagent が使えない、または明示的に bypass された場合だけ、single-worker CLI fallback として \`--run --command\`（つまり \`ospec execute launch ... --run --command "..."\`）を使い、\`artifacts/agents/worker-runs/\` を記録する。その後 \`ospec execute collect [changes/active/<change>] [--task task-id] [--run run-id]\` で fallback task result を記録する。blocked/needs-context/failed work の修正後は \`ospec execute retry\` で再 dispatch する。完了済み task は \`--force\` が必要。
各 worker task 完了後、\`ospec execute review [changes/active/<change>] --task <task-id>\` で統合 task code reviewer packet（spec compliance と code quality を一度に確認）を 1 つ作成する。task-level review decision は \`artifacts/reviews/tasks/<task-id>/review.md\` に保存され、その 1 回の統合 review が承認されるまで dependent task は dispatch されない。
すべての task-level review が承認され task graph が完了した後、\`--task\` なしの \`ospec execute review [changes/active/<change>]\` で \`artifacts/agents/review-dispatches/\` 配下に 1 つの統合 final whole-change code reviewer packet を作成する。これは単一 \`artifacts/reviews/final-review.md\` の decision を書く。
explicit \`ospec execute review ... --run --command "..."\` の場合のみ local reviewer command を実行し、\`artifacts/agents/review-runs/\` を記録する。\`--decision\` がある場合は review decision も書き戻せる。
review artifact が non-\`PENDING\` decision を持つ場合は \`ospec execute feedback [changes/active/<change>] [--stage spec|quality]\` で \`artifacts/agents/review-feedback-plan.json\` と \`artifacts/agents/review-feedback-plan.md\` を書く。追加作業を dispatch する前に accept、revise、clarify、blocked の handling を明確にし、feedback が scope、direction、API、UI、risk、accepted tradeoffs を変える場合は required user decision を作成する。
task graph、execution session、review artifact、debug evidence、verification checklist を手動編集した後は \`ospec execute sync [changes/active/<change>]\` で \`artifacts/agents/worker-status.md\` を再構築する。
\`ospec session\`、\`ospec execute bootstrap\`、\`handoff\`、\`doc-review\`、\`workspace\`、plan-mode \`worktree\`、\`finish\`、\`dispatch\`、\`launch\`、\`collect\`、\`retry\`、\`complete\`、\`review\`、\`feedback\`、\`debug\`、\`tdd\`、\`verify\`、\`sync\` は OSpec artifacts のみを更新する。\`workspace\`、\`worktree\`、\`finish\` が git state を読む場合を除き、project source files は直接編集しない。native subagent は current AI harness が起動する。shell command は explicit \`worktree --create\`、\`worktree --cleanup\`、fallback \`launch --run --command\`、\`review --run --command\`、または fallback \`orchestrate\` の場合のみ実行する。
\`artifacts/agents/task-graph.json\` に未解決の task 状態、無効な依存関係、不足した実行詳細、またはトップレベル \`status\` が \`completed\` でない状態がある場合は archive しない。
実装と review の間は \`artifacts/agents/worker-status.md\` を更新する。worker 状態が \`PENDING\`、\`NEEDS_CONTEXT\`、\`BLOCKED\` のままなら完了を主張しない。
archive 前に \`controller_status\` は \`DONE\` でなければならない。
focused test 実行後、\`ospec execute tdd [changes/active/<change>] --phase red|green|refactor --command "..."\` で \`artifacts/agents/tdd-evidence.json\` を記録するか、適用外の理由を記録する。
debugging が change の一部だった場合、\`ospec execute debug [changes/active/<change>] --phase reproduce|isolate|hypothesize|fix|verify --symptom "..." --root-cause "..." --status FIXED\` で \`artifacts/agents/debug-evidence.json\` を記録するか、適用外の理由を記録する。
fresh project verification commands を実行した後、\`ospec execute verify [changes/active/<change>] --command "..." --status PASSED\` で \`artifacts/agents/verification-evidence.json\` を記録する。
実装後は各 task の 1 回の統合 review を完了し、単一の final \`artifacts/reviews/final-review.md\` を完了する。task-level または final review decision が \`PENDING\`、\`NEEDS_CHANGES\`、\`BLOCKED\` のままなら archive しない。
\`.skillrc.workflow.document_review_policy\` の既定値は \`always\`。\`adaptive\` は対象文書が \`risk_level: low\`（または \`none\`）を明示し risk signal がない場合だけ inline 承認し、それ以外は独立 reviewer を dispatch する。
worker/reviewer packet は logical model profile、configured model、または harness-default fallback を記録する。command runner は \`OSPEC_USAGE_FILE\` を自動集計し、complete/partial/missing coverage を報告する。
reviewer は Markdown と隣接 \`*.findings.json\` を書く。宣言済み documentation update には有意な変更 evidence が必要で、archive index は永続 project document をリンクする。
archive 済み classic change と goal ごとに \`docs/project/changes/<archive-path>.md\` を生成して index する。active change を移動する前に、preflight は同じ path にある人所有の文書の上書きを拒否し、管理対象の出力ディレクトリが書き込み可能であることを確認する。postcondition で文書と両方の index link を確認する。
final review が \`NEEDS_CHANGES\` の場合、required decision 解決後に \`ospec execute repair\` で全 findings を 1 repair task にまとめ、covering verification、task review、final re-review を各 1 回行う。

## プラグインゲート

- まず \`.skillrc.plugins\` を読んで有効なブロック系プラグインを確認する。
- 現在の change が \`tasks.md\` または \`verification.md\` で \`stitch_design_review\` を有効化している場合は、先に \`artifacts/stitch/approval.json\` を確認する。
- \`approval.json.status\` が \`approved\` でない場合、継続実装、完了宣言、archive 準備完了として扱わない。
- Stitch provider、MCP、認証設定が関係する場合は、まずリポジトリ内の Stitch 仕様を読む。仕様がない場合のみ内蔵ベースラインを使う。
- 内蔵 Codex provider が読み取り専用呼び出しでは成功するのに Stitch 書き込みが止まる場合は、\`codex exec --dangerously-bypass-approvals-and-sandbox\` が実際に使われているかを確認する。カスタム runner でも同じフラグが必要。
- 手動レビュー結果を記録する場合は \`ospec plugins approve stitch <change-path>\` または \`ospec plugins reject stitch <change-path>\` を優先する。`, `# بروتوكول التنفيذ

## مصدر الحقيقة

- \`.skillrc\`
- \`.ospec/session-brief.md\`
- \`SKILL.index.json\` و\`docs/project/feature-index.md\`
- ناتج \`ospec execute status --brief\` الحالي أو dispatch/review/decision/verification packet

تعامل مع brief أو packet كسياق المهمة، ثم استخدم الفهارس لفتح change artifacts والملفات المستهدفة ووثائق project/archive اللازمة للمرحلة الحالية فقط. لا تحمّل جميع goal artifacts افتراضياً.

## القاعدة

استخدم وثائق المشروع وملفات SKILL متعددة الطبقات للمعرفة طويلة الأمد.
واستخدم التغييرات النشطة للأعمال التنفيذية الحالية.
عند \`workflow_profile_id: change\` استخدم المسار السريع الكلاسيكي ولا تطلب إلا \`proposal.md\` و\`tasks.md\` و\`state.json\` و\`verification.md\` و\`review.md\`؛ لا تملأ وثائق goal-only لمجرد أنها مذكورة في قالب.
عند \`workflow_profile_id: goal\` استخدم المسار الكامل واطلب \`design.md\` و\`implementation-plan.md\` و\`artifacts/agents/task-graph.json\` وdocument reviews وfinal reviews وworker status وverification evidence.
عند الدخول إلى مشروع OSpec موجود، استخدم \`ospec session [path]\` لكتابة \`.ospec/session-brief.json\` و\`.ospec/session-brief.md\`؛ هذا project entry brief يسجل سياق active change وqueued change وqueue-run وcache fingerprint والأمر الآمن التالي فقط ولا يستبدل \`ospec execute bootstrap\` للـ active change.
تعامل مع خطوات built-in quality policy المفعّلة مثل \`tdd_cycle\` و\`root_cause_debug\` و\`verification_evidence\` كـ \`optional_steps\` خاضعة لـ archive gate؛ غطّها في task documents وverification documents وملفات evidence المطابقة قبل closeout.
عند تنفيذ goal بمساعدة AI، أنشئ \`design.md\` أو حدّثه من المتطلب و\`proposal.md\` وسياق المشروع قبل تعديل \`implementation-plan.md\` أو اشتقاق \`artifacts/agents/task-graph.json\` أو تعديل \`tasks.md\` أو الكود.
\`Announce-Before-Act\`: لا تُشغّل سير العمل بصمت — أعلن أي skill ومرحلة، وأي أمر \`ospec execute ...\` ستشغّله وما يكتبه، وكم subagent أصلي ستوزّع وبأي آلية (\`Task\` لـ Claude Code، و\`spawn_agent\`/\`wait_agent\`/\`close_agent\` لـ Codex/GPT، و\`@generalist\` لـ Gemini، و\`@mention\` لـ OpenCode)، وأي بوابة تحجب التقدم.
\`Brainstorm-First\`: ابدأ كل goal بعصف ذهني قصير قبل تثبيت التصميم، واسأل المستخدم عن الاتجاه والبنية وAPI والبيانات وUI والمخاطر والنطاق واحداً تلو الآخر؛ وفضّل رفع decision gate دائمة على الافتراض الصامت، ولا تسجّل افتراضاً في \`design.md\` إلا عند تفويض المستخدم مع وسمه كافتراض بحاجة لتأكيد.
عندما يجب أن ينتظر change اختيار المستخدم، سجّل durable decision gate عبر \`ospec execute decision [changes/active/<change>] --id <id> --question "..." --option id:label:impact --option id:label:impact [--recommended id] [--required]\`، واعرض \`Chat Prompt\` من decision report أو \`artifacts/agents/decisions/index.md\`، ثم سجّل الإجابة عبر \`ospec execute decision [changes/active/<change>] --id <id> --select <option-id>\`.
يجب اشتقاق \`implementation-plan.md\` من \`design.md\` مع الملفات المستهدفة والنتائج المتوقعة وأوامر التحقق والاعتماديات والعمل القابل للتوازي والتعارضات؛ ويجب اشتقاق \`artifacts/agents/task-graph.json\` من \`implementation-plan.md\`؛ ويجب اشتقاق \`tasks.md\` من task graph.
عند بدء أو استئناف active change واحد، استخدم \`ospec execute bootstrap [changes/active/<change>]\` لكتابة \`artifacts/agents/bootstrap.json\` و\`artifacts/agents/bootstrap.md\` مع project session brief snapshot، ثم اتبع الإجراء الآمن التالي المسجل فيه؛ عند وجود active dispatch، يوصي bootstrap بأمر \`ospec execute launch ... --task ...\` المطابق.
عندما تحتاج إلى حفظ أمر OSpec التالي الموصى به لتسليمه إلى إنسان أو AI، استخدم \`ospec execute route [changes/active/<change>]\` لكتابة \`artifacts/agents/workflow-route.json\` و\`artifacts/agents/workflow-route.md\`؛ هذا يسجل workflow routing artifacts فقط ولا يعدل source files.
عند نقل change بين agents أو tools أو worktrees أو shells أو operators بشريين، استخدم \`ospec execute handoff [changes/active/<change>] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic]\` لكتابة \`artifacts/agents/handoff.json\` و\`artifacts/agents/handoff.md\`؛ يسجل هذا الأمر project session brief snapshot وtool mapping وقواعد السلامة فقط ولا يشغّل workers أو يعدّل source files.
قبل اشتقاق implementation tasks أو dispatch لها، استخدم \`ospec execute doc-review [changes/active/<change>] [--stage design|plan]\` لإنشاء document reviewer packets تتضمن project session brief snapshot تحت \`artifacts/agents/document-review-dispatches/\` وتجهيز \`artifacts/reviews/design-review.md\` أو \`artifacts/reviews/implementation-plan-review.md\`؛ يجب اعتماد design review قبل dispatch لمراجعة implementation plan.
قبل handoff إلى worker، استخدم \`ospec execute workspace [changes/active/<change>]\` لكتابة \`artifacts/agents/workspace-status.json\` و\`artifacts/agents/workspace-status.md\`؛ إذا كانت الحالة \`needs_isolation\` فنظّف workspace أو انقل العمل إلى git worktree معزول قبل parallel dispatch.
قبل إنشاء git worktree معزول، استخدم \`ospec execute worktree [changes/active/<change>] [--branch name] [--path path] [--base ref]\` لكتابة \`artifacts/agents/worktree-plan.json\` و\`artifacts/agents/worktree-plan.md\`؛ هذا الأمر يسجل خطة تحضير فقط ولا يشغّل \`git worktree add\`.
قبل الإغلاق النهائي، استخدم \`ospec execute finish [changes/active/<change>] [--target main] [--remote origin]\` لكتابة \`artifacts/agents/finish-plan.json\` و\`artifacts/agents/finish-plan.md\`؛ هذا الأمر يسجل الجاهزية ونص الأوامر فقط ولا يشغّل finalize أو archive أو push أو merge أو حذف worktree. عندما تكون حالة finish plan هي ready ولا توجد required pending decision، شغّل \`ospec finalize [changes/active/<change>]\`؛ استخدم \`ospec archive ... --check\` كمعاينة dry-run اختيارية فقط ولا تتوقف بعد نجاحها.
استخدم \`ospec execute dispatch [changes/active/<change>] [--task task-id] [--limit N]\` لإنشاء batch آمن للتوازي من worker packets و\`artifacts/agents/execution-session.json\`؛ يتضمن كل packet project session brief snapshot وworker profile يوضح capability tier وrecommended target وtarget tool mapping وrationale وrequired behavior؛ واستخدم \`ospec execute complete <task-id> ...\` لتسجيل worker results؛ استخدم \`--task\` لمهمة واحدة صريحة و\`--limit\` لتحديد حجم dispatch batch؛ وعندما يسجل \`complete\` النتيجة \`NEEDS_CONTEXT\` أو \`BLOCKED\` يتم إنشاء \`artifacts/agents/blockers/\`.
بعد dispatch، استخدم \`ospec execute launch [changes/active/<change>] [--task task-id] [--target codex|gpt|claude|gemini|opencode|cursor|copilot|shell|generic] [--dry-run] [--json]\` لكتابة native agent launch plan؛ يتطلب active dispatch واحدا وحالة workspace ready، واستخدم \`--json\` عندما يحتاج adapter إلى machine-readable launch artifact على stdout، ويوضح للـ AI المتحكم آلية harness الأصلية: Codex/GPT عبر \`spawn_agent\`/\`wait_agent\`/\`close_agent\`، وClaude Code عبر Task، وGemini عبر \`@generalist\`، وOpenCode عبر \`@mention\`، وCursor عبر Agent/task chat، وCopilot عبر CLI/coding-agent task.
مسار multi-worker الافتراضي هو native subagents في current harness: أنشئ parallel-safe packets، وافحص \`artifacts/agents/launch-plan.md\`، ثم شغّل native agent واحدا لكل packet آمن، وسجل كل نتيجة عبر \`ospec execute complete\`.
استخدم \`ospec execute orchestrate [changes/active/<change>] --command "..." [--limit N] [--max-rounds N] [--timeout-ms N]\` فقط كآخر CLI fallback عندما لا يدعم current AI harness native subagents؛ يرندر fallback explicit command template، ويشغّل worker commands بالتوازي، ويسجل \`artifacts/agents/orchestration-runs/\`، ثم يجمع النتائج إلى task graph ويعرض failed-worker retry commands.
استخدم \`--run --command\` (أي \`ospec execute launch ... --run --command "..."\`) فقط كـ single-worker CLI fallback عندما لا تتوفر native subagents أو يتم تجاوزها صراحة؛ يسجل ذلك \`artifacts/agents/worker-runs/\`. بعدها استخدم \`ospec execute collect [changes/active/<change>] [--task task-id] [--run run-id]\` لتسجيل fallback task result. بعد إصلاح blocked أو needs-context أو failed work استخدم \`ospec execute retry\` لإعادة dispatch؛ المهام المكتملة تحتاج \`--force\`.
بعد اكتمال كل worker task، استخدم \`ospec execute review [changes/active/<change>] --task <task-id>\` لإنشاء حزمة code reviewer موحدة واحدة (spec compliance وcode quality في تمريرة واحدة)؛ يحفظ قرار task-level في \`artifacts/reviews/tasks/<task-id>/review.md\` وتبقى المهام التابعة محجوبة حتى اعتماد تلك المراجعة الموحدة الواحدة.
بعد اعتماد كل task-level reviews واكتمال task graph، استخدم \`ospec execute review [changes/active/<change>]\` من دون \`--task\` لإنشاء حزمة code reviewer نهائية موحدة واحدة تتضمن project session brief snapshot تحت \`artifacts/agents/review-dispatches/\`؛ تكتب قرار \`artifacts/reviews/final-review.md\` واحدا.
لا يشغّل OSpec local reviewer command إلا عند استخدام \`ospec execute review ... --run --command "..."\` صراحة؛ يسجل ذلك \`artifacts/agents/review-runs/\` ويمكنه كتابة review decision عند تمرير \`--decision\`.
بعد أن يحتوي review artifact على قرار غير \`PENDING\`، استخدم \`ospec execute feedback [changes/active/<change>] [--stage spec|quality]\` لكتابة \`artifacts/agents/review-feedback-plan.json\` و\`artifacts/agents/review-feedback-plan.md\`؛ حدد accept أو revise أو clarify أو blocked قبل dispatch عمل إضافي، وأنشئ required user decision عندما يغير feedback scope أو direction أو API أو UI أو risk أو accepted tradeoffs.
بعد تعديل task graph أو execution session أو review artifact أو debug evidence أو verification checklist يدويا، استخدم \`ospec execute sync [changes/active/<change>]\` لإعادة بناء \`artifacts/agents/worker-status.md\`.
\`ospec session\` و\`ospec execute bootstrap\` و\`handoff\` و\`doc-review\` و\`workspace\` وplan-mode \`worktree\` و\`finish\` و\`dispatch\` و\`launch\` و\`collect\` و\`retry\` و\`complete\` و\`review\` و\`feedback\` و\`debug\` و\`tdd\` و\`verify\` و\`sync\` تحدّث OSpec artifacts فقط؛ وباستثناء قراءة \`workspace\` و\`worktree\` و\`finish\` لحالة git، لا تعدّل project source files مباشرة. يتم تشغيل native subagents بواسطة current AI harness؛ ولا تعمل أوامر shell إلا مع explicit \`worktree --create\` أو \`worktree --cleanup\` أو fallback \`launch --run --command\` أو \`review --run --command\` أو fallback \`orchestrate\`.
لا تؤرشف عندما يحتوي \`artifacts/agents/task-graph.json\` على حالات مهام غير محسومة أو اعتماديات غير صالحة أو تفاصيل تنفيذ ناقصة أو عندما لا يكون \`status\` العلوي \`completed\`.
أثناء التنفيذ والمراجعة، حدّث \`artifacts/agents/worker-status.md\`؛ ولا تدّعِ الاكتمال ما دامت أي حالة worker هي \`PENDING\` أو \`NEEDS_CONTEXT\` أو \`BLOCKED\`.
ويجب أن تكون \`controller_status\` هي \`DONE\` قبل الأرشفة.
بعد تشغيل focused tests، سجل \`artifacts/agents/tdd-evidence.json\` باستخدام \`ospec execute tdd [changes/active/<change>] --phase red|green|refactor --command "..."\` أو سجل سبب عدم الانطباق.
عندما يكون debugging جزءا من change، سجل \`artifacts/agents/debug-evidence.json\` باستخدام \`ospec execute debug [changes/active/<change>] --phase reproduce|isolate|hypothesize|fix|verify --symptom "..." --root-cause "..." --status FIXED\` أو سجل سبب عدم الانطباق.
بعد تشغيل project verification commands حديثة، سجل \`artifacts/agents/verification-evidence.json\` باستخدام \`ospec execute verify [changes/active/<change>] --command "..." --status PASSED\`.
بعد التنفيذ، أكمل المراجعة الموحدة الواحدة لكل task، ثم أكمل \`artifacts/reviews/final-review.md\` النهائية الموحدة؛ ولا تؤرشف ما دام أي task-level أو final review decision هو \`PENDING\` أو \`NEEDS_CHANGES\` أو \`BLOCKED\`.
القيمة الافتراضية لـ \`.skillrc.workflow.document_review_policy\` هي \`always\`؛ لا يعتمد \`adaptive\` inline إلا عندما تصرح الوثيقة بـ \`risk_level: low\` (أو \`none\`) ولا توجد risk signals، وإلا يوزع reviewer مستقلا.
تسجل worker/reviewer packets logical model profile أو configured model أو harness-default fallback؛ ويجمع command runner \`OSPEC_USAGE_FILE\` تلقائيا ويبلغ تغطية complete/partial/missing.
يكتب reviewer ملف Markdown وملف \`*.findings.json\` مجاورا؛ وتتطلب documentation updates المعلنة دليلا على تغير فعلي، ويربط archive index وثائق المشروع الدائمة.
يجب إنشاء وفهرسة \`docs/project/changes/<archive-path>.md\` لكل classic change وgoal مؤرشف. قبل نقل change النشط، يجب أن يرفض preflight الكتابة فوق وثيقة يملكها البشر في المسار نفسه وأن يتحقق من قابلية الكتابة في أدلة الإخراج المُدارة، ويتحقق postcondition من الوثيقة وروابط الفهرسين.
عندما يكون final review هو \`NEEDS_CHANGES\`، حل required decisions ثم استخدم \`ospec execute repair\` لتجميع كل findings في repair task واحدة، ثم verification وtask review وfinal re-review مرة واحدة لكل منها.

## بوابات الإضافات

- اقرأ \`.skillrc.plugins\` أولاً لاكتشاف الإضافات الحاجبة المفعلة.
- إذا كان change الحالي يفعّل \`stitch_design_review\` في \`tasks.md\` أو \`verification.md\`، فافحص \`artifacts/stitch/approval.json\` قبل التقدم.
- عندما لا تكون \`approval.json.status\` مساوية لـ \`approved\`، فلا تعتبر change جاهزاً للاستمرار أو إعلان الاكتمال أو الأرشفة.
- إذا كان العمل يتضمن Stitch provider أو MCP أو إعدادات المصادقة، فاقرأ مواصفة Stitch المحلية في المستودع أولاً، واستخدم الخطوط الأساسية المدمجة فقط عند غيابها.
- إذا نجح provider الداخلي لـ Codex في الاستدعاءات للقراءة فقط لكن توقفت عمليات كتابة Stitch، فتحقق أولاً من أن التشغيل يستخدم فعلاً \`codex exec --dangerously-bypass-approvals-and-sandbox\`. وإذا استُخدم runner مخصص فيجب أن يمرر العلم نفسه.
- عند تسجيل قرارات المراجعة اليدوية، فضّل \`ospec plugins approve stitch <change-path>\` أو \`ospec plugins reject stitch <change-path>\`.`));
    }
    generateBuildIndexScriptTemplate() {
        const runtimeTool = path.resolve(__dirname, '..', '..', 'tools', 'build-index.js');
        return (0, fs_1.readFileSync)(runtimeTool, 'utf8');
    }
    getProjectContext(fallbackName, mode, input) {
        return this.inputs.normalizeProjectBootstrapInput(input, fallbackName, mode);
    }
    getPresetModuleSkillBody(context, moduleName, slug, refs) {
        if (context.projectPresetId !== 'official-site' ||
            (context.documentLanguage !== 'zh-CN' && context.documentLanguage !== 'en-US')) {
            return null;
        }
        const moduleKey = slug.toLowerCase();
        const docsLink = this.formatReferenceList(refs, this.copy(context.documentLanguage, '待补充', 'TBD'));
        const relatedDocs = this.isEnglish(context.documentLanguage)
            ? `- Module map: [../../../../docs/project/module-map.md](../../../../docs/project/module-map.md)
- API overview: [../../../../docs/project/api-overview.md](../../../../docs/project/api-overview.md)`
            : `- 项目模块地图：[../../../../docs/project/module-map.md](../../../../docs/project/module-map.md)
- API 总览：[../../../../docs/project/api-overview.md](../../../../docs/project/api-overview.md)`;
        const templates = {
            web: {
                zh: `# ${moduleName} 模块

> 层级：第 3 层（业务模块文档）
> 上层：[knowledge/src/SKILL.md](../../SKILL.md)

## 模块定位

- 负责官网首页、全局导航和关键 CTA 的首屏体验
- 串联 docs、blog、admin、login 等主入口
- 承担品牌展示、信息分发和主站级布局约束

## 主要职责

- 维护首页、导航、页脚与站点级布局
- 暴露首页到 docs/blog/admin/login 的稳定跳转关系
- 与 content 模块协作，消费首屏展示所需的内容摘要

## API 文档

${docsLink}

## 交付边界

- 首页 Hero、功能区、入口区块和页脚属于 web 模块
- 站点级路由编排、导航配置和营销内容组合属于 web 模块
- 具体文档内容、博客内容、后台内容工作流不在此模块内部实现

## 测试要求

- 单元测试：导航配置、首页区块配置、入口卡片等静态装配逻辑
- 集成测试：首页到 docs/blog/admin/login 的关键跳转与路由装配
- 回归验证：主导航或首页结构调整后同步验证官网 MVP change

## 关联文档

${relatedDocs}`,
                en: `# ${moduleName}

> Layer: module document
> Parent: [knowledge/src/SKILL.md](../../SKILL.md)

## Module Positioning

- Own the homepage, global navigation, and primary CTA experience
- Connect docs, blog, admin, and login entry points
- Hold brand presentation, information hierarchy, and site-wide layout constraints

## Responsibilities

- Maintain the homepage, navigation, footer, and global shell
- Expose stable links from the homepage into docs, blog, admin, and login
- Consume content summaries from the content module for marketing surfaces

## API Docs

${docsLink}

## Delivery Boundaries

- Hero, feature sections, entry cards, and footer belong to the web module
- Site routing composition, navigation config, and marketing assembly belong here
- Detailed docs/blog content and admin workflows do not live inside this module

## Testing Requirements

- Unit tests: navigation config, homepage section assembly, CTA cards
- Integration tests: critical routing across home, docs, blog, admin, and login
- Regression checks: validate the official-site MVP change after navigation updates

## Related Docs

${relatedDocs}`,
            },
            docs: {
                zh: `# ${moduleName} 模块

> 层级：第 3 层（业务模块文档）
> 上层：[knowledge/src/SKILL.md](../../SKILL.md)

## 模块定位

- 负责文档中心的信息架构、目录层级与阅读入口
- 承接从首页进入 docs 的主路径
- 与 content 模块协作，定义文档内容的展示形态

## 主要职责

- 维护文档首页、文档侧边栏、分类入口和阅读页骨架
- 定义文档版本、栏目、目录层级的呈现结构
- 约束搜索、上一篇/下一篇、返回导航等阅读辅助能力

## API 文档

${docsLink}

## 交付边界

- 文档目录结构、阅读布局和导航关系由 docs 模块负责
- 原始文档实体、发布状态和内容获取策略由 content 模块负责
- 后台内容编辑能力由 admin 模块负责

## 测试要求

- 单元测试：目录映射、文档导航、阅读辅助逻辑
- 集成测试：首页进入 docs、文档列表到文档详情的主链路
- 回归验证：文档信息架构调整后同步验证导航与内容落点

## 关联文档

${relatedDocs}`,
                en: `# ${moduleName}

> Layer: module document
> Parent: [knowledge/src/SKILL.md](../../SKILL.md)

## Module Positioning

- Own the docs-center information architecture, hierarchy, and reading entry points
- Carry the primary route from the homepage into docs
- Work with the content module to shape how documentation content is presented

## Responsibilities

- Maintain the docs home, sidebar, category entry points, and reading-page shell
- Define the structure for versions, sections, and content hierarchy
- Constrain search, prev/next navigation, and reading-assistance behavior

## API Docs

${docsLink}

## Delivery Boundaries

- Docs layout, reading shell, and navigation structure belong to this module
- Source document entities and publication state belong to the content module
- Admin editing flows belong to the admin module

## Testing Requirements

- Unit tests: hierarchy mapping, sidebar navigation, reading helpers
- Integration tests: homepage-to-docs entry and docs-list-to-doc-detail flow
- Regression checks: revalidate navigation and content placement after IA changes

## Related Docs

${relatedDocs}`,
            },
            content: {
                zh: `# ${moduleName} 模块

> 层级：第 3 层（业务模块文档）
> 上层：[knowledge/src/SKILL.md](../../SKILL.md)

## 模块定位

- 负责官网内容模型、内容读取边界和发布状态
- 为 web、docs、blog 与 admin 模块提供统一内容来源
- 约束文档、博客、更新日志等内容实体的一致性

## 主要职责

- 定义 docs、blog、changelog、site settings 等核心内容实体
- 管理草稿、已发布、预览等内容状态
- 提供内容摘要、详情和列表所需的装配逻辑

## API 文档

${docsLink}

## 交付边界

- 内容实体、字段约束、slug、发布日期和标签等属于 content 模块
- 页面视觉布局不在此模块内部完成
- 后台编辑界面与审批流在 admin 模块中承接

## 测试要求

- 单元测试：内容模型映射、状态转换、列表/详情装配
- 集成测试：docs/blog/changelog 的读取与预览链路
- 回归验证：内容字段或状态机调整后同步验证页面消费方

## 关联文档

${relatedDocs}`,
                en: `# ${moduleName}

> Layer: module document
> Parent: [knowledge/src/SKILL.md](../../SKILL.md)

## Module Positioning

- Own the official-site content model, delivery boundaries, and publication state
- Serve as the shared content source for web, docs, blog, and admin surfaces
- Keep documentation, blog, and changelog entities consistent

## Responsibilities

- Define docs, blog, changelog, and site-setting entities
- Manage draft, published, and preview states
- Provide the assembly logic behind content lists, summaries, and detail views

## API Docs

${docsLink}

## Delivery Boundaries

- Entities, field constraints, slugs, publish dates, and tags belong here
- Visual page composition does not belong inside this module
- Admin editing and approval flows are handled by the admin module

## Testing Requirements

- Unit tests: model mapping, state transitions, list/detail assembly
- Integration tests: docs/blog/changelog delivery and preview flow
- Regression checks: validate all consuming surfaces after schema changes

## Related Docs

${relatedDocs}`,
            },
            admin: {
                zh: `# ${moduleName} 模块

> 层级：第 3 层（业务模块文档）
> 上层：[knowledge/src/SKILL.md](../../SKILL.md)

## 模块定位

- 负责官网内容管理后台、运营入口和内容发布操作台
- 承接内容编辑、预览、发布与回滚相关工作流
- 与 auth 模块协作，确保后台访问边界清晰

## 主要职责

- 维护后台首页、内容列表、内容编辑、发布操作入口
- 定义后台所需的角色权限边界和操作日志关注点
- 承接内容审核、发布、撤回和预览流程

## API 文档

${docsLink}

## 交付边界

- admin 模块只负责后台工作流与管理入口，不负责公开站点渲染
- 内容实体本身由 content 模块负责
- 登录、会话与鉴权校验由 auth 模块负责

## 测试要求

- 单元测试：后台菜单、内容状态动作、操作约束
- 集成测试：登录后进入后台、编辑内容、预览和发布链路
- 回归验证：权限边界与内容状态切换需要与 auth/content 同步验证

## 关联文档

${relatedDocs}`,
                en: `# ${moduleName}

> Layer: module document
> Parent: [knowledge/src/SKILL.md](../../SKILL.md)

## Module Positioning

- Own the admin workspace, operator entry points, and publishing console
- Carry editing, preview, publish, and rollback workflows
- Work with the auth module to keep admin access boundaries explicit

## Responsibilities

- Maintain the admin home, content list, editor flow, and publish actions
- Define role-sensitive boundaries and operator-facing audit concerns
- Handle review, publish, rollback, and preview steps

## API Docs

${docsLink}

## Delivery Boundaries

- This module owns the admin workflow and management surface, not public rendering
- Content entities themselves belong to the content module
- Login/session enforcement belongs to the auth module

## Testing Requirements

- Unit tests: menu config, content-state actions, admin guard behavior
- Integration tests: login-to-admin, edit-to-preview, and publish flow
- Regression checks: validate auth/content boundaries after workflow changes

## Related Docs

${relatedDocs}`,
            },
            auth: {
                zh: `# ${moduleName} 模块

> 层级：第 3 层（业务模块文档）
> 上层：[knowledge/src/SKILL.md](../../SKILL.md)

## 模块定位

- 负责官网登录、会话状态和后台访问保护
- 为 admin 模块提供权限门禁，为 web 模块提供登录入口
- 统一处理用户身份、重定向与会话校验

## 主要职责

- 登录页、退出登录、会话恢复和路由守卫
- 后台入口鉴权、未登录跳转和基础角色判断
- 与 auth api 协作，落地会话创建、读取和销毁

## API 文档

${docsLink}

## 交付边界

- 登录流程、会话状态、守卫逻辑属于 auth 模块
- 用户内容资料和运营内容不在此模块内部维护
- 更复杂的 RBAC 与多组织模型不属于官网初始化范围

## 测试要求

- 单元测试：会话状态、守卫逻辑、登录跳转
- 集成测试：login -> admin、会话失效 -> 重定向、退出登录链路
- 回归验证：权限边界变化后同步验证 admin 页面可达性

## 关联文档

${relatedDocs}`,
                en: `# ${moduleName}

> Layer: module document
> Parent: [knowledge/src/SKILL.md](../../SKILL.md)

## Module Positioning

- Own login, session state, and admin access protection
- Provide admin gating for the admin module and login entry points for the web module
- Centralize identity, redirects, and session validation

## Responsibilities

- Login/logout flows, session restore, and route guards
- Admin access checks, unauthenticated redirects, and basic role handling
- Integrate with the auth API for session create/read/destroy flows

## API Docs

${docsLink}

## Delivery Boundaries

- Login flow, session state, and guard logic belong to this module
- User profile content and editorial content do not belong here
- Advanced RBAC or multi-tenant models are out of scope for bootstrap

## Testing Requirements

- Unit tests: session state, guard logic, redirect behavior
- Integration tests: login-to-admin, expired-session redirect, logout flow
- Regression checks: revalidate admin reachability after auth-boundary changes

## Related Docs

${relatedDocs}`,
            },
        };
        const template = templates[moduleKey];
        if (!template) {
            return null;
        }
        return this.isEnglish(context.documentLanguage) ? template.en : template.zh;
    }
    getPresetApiAreaDocBody(context, apiAreaName) {
        if (context.projectPresetId !== 'official-site' ||
            (context.documentLanguage !== 'zh-CN' && context.documentLanguage !== 'en-US')) {
            return null;
        }
        const key = apiAreaName.trim().toLowerCase();
        const templates = {
            'content api': {
                zh: `# ${apiAreaName}

## 项目

- **项目名称**：${context.projectName}

## 边界说明

- 提供 docs、blog、changelog、站点配置等内容读取能力
- 区分公开读取、预览读取和后台内容操作的接口边界
- 维护 slug、发布时间、标签、草稿态等内容语义

## 建议接口分组

- 内容列表：docs / blog / changelog 列表与筛选
- 内容详情：按 slug 获取详情、相邻内容、关联内容
- 预览接口：供 admin 模块预览草稿内容
- 站点配置：导航、页脚、公告位等站点级内容

## 待补充清单

- [ ] 路由列表
- [ ] 请求/响应结构
- [ ] 预览权限与缓存策略
- [ ] 测试与验收要求`,
                en: `# ${apiAreaName}

## Project

- **Project**: ${context.projectName}

## Boundary

- Provide content delivery for docs, blog, changelog, and site settings
- Separate public delivery, preview delivery, and admin-side content operations
- Own slugs, publication dates, tags, and draft/published semantics

## Suggested Interface Groups

- Content lists: docs/blog/changelog list and filtering
- Content detail: fetch by slug with adjacent/related content
- Preview interfaces: allow admin previews for draft content
- Site settings: navigation, footer, and announcement content

## Checklist

- [ ] Routes
- [ ] Request / response payloads
- [ ] Preview permissions and cache strategy
- [ ] Tests and acceptance requirements`,
            },
            'auth api': {
                zh: `# ${apiAreaName}

## 项目

- **项目名称**：${context.projectName}

## 边界说明

- 提供登录、会话读取、退出登录与后台访问鉴权能力
- 为 admin 页面和登录页提供统一的身份状态接口
- 约束未登录跳转、会话失效与基础角色判定

## 建议接口分组

- 登录接口：用户名/邮箱 + 密码或其他登录方式
- 会话接口：当前会话读取、刷新、失效处理
- 退出接口：会话销毁、清理与跳转
- 后台守卫：校验是否允许进入 admin 相关页面

## 待补充清单

- [ ] 路由列表
- [ ] 请求/响应结构
- [ ] Cookie / token 策略
- [ ] 测试与验收要求`,
                en: `# ${apiAreaName}

## Project

- **Project**: ${context.projectName}

## Boundary

- Provide login, session read, logout, and admin-access enforcement
- Expose a shared identity surface for admin pages and login flows
- Define unauthenticated redirects, session expiry handling, and basic role checks

## Suggested Interface Groups

- Login: username/email and credential entry
- Session: read, refresh, and expiry handling
- Logout: session destroy and redirect behavior
- Admin guards: verify whether admin access is allowed

## Checklist

- [ ] Routes
- [ ] Request / response payloads
- [ ] Cookie / token strategy
- [ ] Tests and acceptance requirements`,
            },
            'admin api': {
                zh: `# ${apiAreaName}

## 项目

- **项目名称**：${context.projectName}

## 边界说明

- 提供内容管理后台所需的编辑、发布、回滚和预览操作接口
- 衔接 admin 模块的操作台与 content 模块的内容模型
- 记录后台动作的权限边界和审核要求

## 建议接口分组

- 内容管理：创建、更新、删除、归档内容
- 状态流转：草稿、送审、发布、撤回
- 预览入口：生成预览链接和预览上下文
- 操作审计：关键操作日志、操作者信息、发布时间

## 待补充清单

- [ ] 路由列表
- [ ] 请求/响应结构
- [ ] 权限与审核规则
- [ ] 测试与验收要求`,
                en: `# ${apiAreaName}

## Project

- **Project**: ${context.projectName}

## Boundary

- Provide the edit, publish, rollback, and preview actions needed by the admin workspace
- Connect the admin workspace to the content model owned by the content module
- Define permission boundaries and approval expectations for operator actions

## Suggested Interface Groups

- Content management: create, update, delete, and archive content
- State transitions: draft, review, publish, and unpublish
- Preview entry: create preview links and preview context
- Audit trail: operator identity, timestamps, and action logs

## Checklist

- [ ] Routes
- [ ] Request / response payloads
- [ ] Permissions and review rules
- [ ] Tests and acceptance requirements`,
            },
        };
        const template = templates[key];
        if (!template) {
            return null;
        }
        return this.isEnglish(context.documentLanguage) ? template.en : template.zh;
    }
    getPresetModuleApiDocBody(context, moduleName, slug) {
        if (context.projectPresetId !== 'official-site' ||
            (context.documentLanguage !== 'zh-CN' && context.documentLanguage !== 'en-US')) {
            return null;
        }
        const docs = {
            web: {
                zh: `# ${moduleName} Module API

## 项目

- **项目名称**：${context.projectName}
- **模块名**：${moduleName}
- **模块路径**：src/modules/${slug}

## 对外接口

- 导航配置：站点主导航、页脚导航、首页入口卡片
- 首页编排：Hero、能力介绍、内容入口、CTA 区块装配
- 页面装配：首页对 docs/blog/admin/login 的路由出口

## 依赖关系

- 上游依赖：content 模块提供内容摘要，auth 模块提供登录态入口信息
- 下游调用方：app/page.tsx、全局布局、站点导航组件
- 共享依赖：路由常量、站点配置、内容摘要类型

## 测试要求

- 单元测试：导航配置、首页区块装配
- 集成测试：首页到 docs/blog/admin/login 的跳转
- 回归验证：站点入口调整后校验官网 MVP 主链路`,
                en: `# ${moduleName} Module API

## Project

- **Project**: ${context.projectName}
- **Module**: ${moduleName}
- **Module path**: src/modules/${slug}

## Public Interface

- Navigation config: primary nav, footer nav, homepage entry cards
- Homepage composition: hero, capability sections, content entry blocks, CTA assembly
- Route composition: public exit points into docs, blog, admin, and login

## Dependencies

- Upstream: content summaries from the content module, auth entry signals from auth
- Downstream consumers: app/page.tsx, global shell, navigation components
- Shared dependencies: route constants, site config, content summary types

## Testing Requirements

- Unit tests: navigation config and homepage section assembly
- Integration tests: home-to-docs/blog/admin/login routing
- Regression checks: validate the official-site MVP flow after shell updates`,
            },
        };
        const template = docs[slug.toLowerCase()];
        if (!template) {
            return null;
        }
        return this.isEnglish(context.documentLanguage) ? template.en : template.zh;
    }
    getPresetDesignDocBody(context, docName) {
        if (context.projectPresetId !== 'official-site' ||
            (context.documentLanguage !== 'zh-CN' && context.documentLanguage !== 'en-US')) {
            return null;
        }
        const key = docName.trim().toLowerCase();
        const isChinese = context.documentLanguage === 'zh-CN';
        if (['ui information architecture', '界面信息架构'].includes(key)) {
            return isChinese
                ? `# ${docName}

## 项目

- **项目名称**：${context.projectName}

## 设计目标

- 明确官网首页、docs、blog、admin、login 五个主入口的页面关系
- 明确首页到各内容面的导航层级和返回路径
- 保证营销展示、文档阅读、后台运营三种体验的边界清晰

## 核心页面

- 首页：品牌表达、核心能力、主入口导航
- docs：文档中心首页、分类页、文档阅读页
- blog：博客/更新日志列表与详情
- admin：内容管理后台首页、内容列表、编辑与发布入口
- login：登录与会话恢复入口

## 待验证问题

- 顶部导航与侧边导航如何避免重复信息
- docs/blog/admin 三个入口的优先级如何表达
- 未登录访问 admin 时的跳转与返回路径如何设计`
                : `# ${docName}

## Project

- **Project**: ${context.projectName}

## Design Goals

- Define the relationship across home, docs, blog, admin, and login
- Clarify the navigation hierarchy from the homepage into each content surface
- Keep marketing, reading, and admin experiences clearly separated

## Core Surfaces

- Home: brand expression, key capabilities, primary entry navigation
- Docs: docs home, category pages, reading pages
- Blog: changelog/blog list and detail views
- Admin: dashboard, content list, editor, and publish entry
- Login: login and session-restore entry

## Open Validation Questions

- How should top navigation and sidebar navigation avoid duplicate signals?
- How should docs/blog/admin priority be communicated from the homepage?
- What should the admin redirect and return path look like for unauthenticated users?`;
        }
        if (['content model', '内容模型'].includes(key)) {
            return isChinese
                ? `# ${docName}

## 项目

- **项目名称**：${context.projectName}

## 设计目标

- 统一官网 docs、blog、changelog 与站点配置的内容实体
- 明确草稿、预览、已发布等状态语义
- 为后台内容管理与公开站点消费提供稳定字段约束

## 核心实体

- DocPage：文档页面、目录、版本、slug
- BlogPost：博客/更新日志文章、摘要、发布时间、标签
- SiteConfig：导航、页脚、公告位、首页重点内容
- Author / Tag：作者与标签等复用内容

## 关键约束

- 所有公开内容都必须具备 slug、状态、最后更新时间
- 预览内容不能被公开缓存
- docs 与 blog 虽可共用字段，但必须保留独立内容类型`
                : `# ${docName}

## Project

- **Project**: ${context.projectName}

## Design Goals

- Unify docs, blog, changelog, and site-setting entities for the official site
- Define draft, preview, and published semantics
- Provide stable field constraints for both admin workflows and public rendering

## Core Entities

- DocPage: document page, hierarchy, version, slug
- BlogPost: blog/changelog post, summary, publish date, tags
- SiteConfig: navigation, footer, announcement slots, homepage highlights
- Author / Tag: reusable author and taxonomy entities

## Key Constraints

- Every public content entity must have slug, status, and last-updated metadata
- Preview content must never be publicly cached
- Docs and blog may share fields, but must remain separate content types`;
        }
        if (['cms workflow', 'cms 工作流'].includes(key)) {
            return isChinese
                ? `# ${docName}

## 项目

- **项目名称**：${context.projectName}

## 设计目标

- 规范后台内容从创建到发布的最小可演示流程
- 明确编辑、预览、送审、发布、撤回之间的状态转换
- 保证后台动作与公开站点的内容可见性一致

## 工作流阶段

- 草稿：编辑中，仅后台可见
- 预览：生成预览上下文，供内部确认
- 待发布：内容已冻结，等待操作人确认
- 已发布：公开站点可见
- 已撤回：公开站点不可见，但保留历史记录

## 验证重点

- 后台操作是否正确驱动 content 状态变化
- 预览内容是否与公开内容严格隔离
- 发布和撤回后首页、docs、blog 是否同步更新`
                : `# ${docName}

## Project

- **Project**: ${context.projectName}

## Design Goals

- Define the smallest demonstrable editorial flow from creation to publish
- Make the transitions between edit, preview, review, publish, and rollback explicit
- Keep admin actions and public-site visibility consistent

## Workflow Stages

- Draft: editable and admin-only
- Preview: generates internal preview context
- Ready to publish: content is frozen and awaiting operator confirmation
- Published: publicly visible
- Unpublished / rolled back: hidden from the public site with history retained

## Validation Focus

- Whether admin actions correctly drive content-state transitions
- Whether preview content remains isolated from public delivery
- Whether home/docs/blog refresh consistently after publish and rollback`;
        }
        return null;
    }
    getPresetPlanningDocBody(context, docName) {
        if (context.projectPresetId !== 'official-site' ||
            (context.documentLanguage !== 'zh-CN' && context.documentLanguage !== 'en-US')) {
            return null;
        }
        const key = docName.trim().toLowerCase();
        const isChinese = context.documentLanguage === 'zh-CN';
        if (['delivery plan', '交付计划'].includes(key)) {
            return isChinese
                ? `# ${docName}

## 项目

- **项目名称**：${context.projectName}

## 规划目标

- 先完成官网 MVP 的页面骨架、内容链路和后台入口
- 再补齐内容工作流、鉴权与上线准备

## 建议里程碑

- M1：首页、docs、blog、admin、login 五个入口可达
- M2：content 模型与 content api 成型，docs/blog 能读取内容
- M3：admin 工作台支持预览、发布与回滚的最小链路
- M4：上线检查清单完成，进入发布准备

## 风险与依赖

- 内容模型与页面结构若先后失配，会导致前后端重复返工
- admin 与 auth 边界若过晚定义，会影响后台主链路联调
- SEO、分析、部署等上线项不能拖到最后才补`
                : `# ${docName}

## Project

- **Project**: ${context.projectName}

## Planning Goal

- Deliver the official-site MVP shell, content flow, and admin entry first
- Then harden content workflow, auth, and launch readiness

## Suggested Milestones

- M1: home, docs, blog, admin, and login entry points are all reachable
- M2: content model and content API are in place for docs/blog delivery
- M3: admin supports the minimum preview/publish/rollback loop
- M4: launch checklist is complete and release prep begins

## Risks and Dependencies

- If content modeling and page structure drift apart, rework will multiply
- If admin/auth boundaries are delayed, the operator flow will stall
- SEO, analytics, and deployment tasks cannot be postponed to the very end`;
        }
        if (['launch checklist', '上线检查清单'].includes(key)) {
            return isChinese
                ? `# ${docName}

## 项目

- **项目名称**：${context.projectName}

## 检查目标

- 在官网发布前统一检查内容、导航、后台、鉴权和基础运维准备

## 上线前检查项

- [ ] 首页、docs、blog、admin、login 五个入口均可访问
- [ ] docs/blog 的内容来源、slug 和 SEO 基础字段已检查
- [ ] admin 登录、会话恢复、退出登录链路可用
- [ ] 预览内容不会被公开访问或缓存
- [ ] 关键页面具备基础埋点、错误页和空态处理
- [ ] 发布与回滚流程经过至少一轮 smoke 验证`
                : `# ${docName}

## Project

- **Project**: ${context.projectName}

## Checklist Goal

- Align content, navigation, admin, auth, and operational readiness before launch

## Pre-launch Checks

- [ ] Home, docs, blog, admin, and login entry points are reachable
- [ ] docs/blog content source, slug rules, and base SEO fields are reviewed
- [ ] admin login, session restore, and logout flows work correctly
- [ ] Preview content is never publicly reachable or cached
- [ ] Key pages have baseline analytics, empty states, and error handling
- [ ] Publish and rollback have passed at least one smoke validation round`;
        }
        return null;
    }
    slugify(value) {
        return value
            .toLowerCase()
            .replace(/\.md$/i, '')
            .replace(/[^a-z0-9]+/g, '-')
            .replace(/^-+|-+$/g, '');
    }
}
exports.ProjectTemplateBuilder = ProjectTemplateBuilder;
